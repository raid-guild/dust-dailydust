// SPDX-License-Identifier: MIT
pragma solidity >=0.8.24;

/* Autogenerated file. Do not edit manually. */

import { ForceFieldProgram } from "../../ForceFieldProgram.sol";
import { HookContext, IProgramValidator, IEnergize, IHit, IAddFragment, IRemoveFragment, IBuild, IMine } from "@dust/world/src/ProgramHooks.sol";
import { revertWithBytes } from "@latticexyz/world/src/revertWithBytes.sol";
import { IWorldCall } from "@latticexyz/world/src/IWorldKernel.sol";
import { SystemCall } from "@latticexyz/world/src/SystemCall.sol";
import { WorldContextConsumerLib } from "@latticexyz/world/src/WorldContext.sol";
import { Systems } from "@latticexyz/world/src/codegen/tables/Systems.sol";
import { ResourceId } from "@latticexyz/store/src/ResourceId.sol";
import { StoreSwitch } from "@latticexyz/store/src/StoreSwitch.sol";

type ForceFieldProgramType is bytes32;

// equivalent to WorldResourceIdLib.encode({ typeId: RESOURCE_SYSTEM, namespace: "template", name: "ForceFieldProgra" }))
ForceFieldProgramType constant forceFieldProgram = ForceFieldProgramType.wrap(
  0x737974656d706c617465000000000000466f7263654669656c6450726f677261
);

struct CallWrapper {
  ResourceId systemId;
  address from;
}

struct RootCallWrapper {
  ResourceId systemId;
  address from;
}

/**
 * @title ForceFieldProgramLib
 * @author MUD (https://mud.dev) by Lattice (https://lattice.xyz)
 * @dev This library is automatically generated from the corresponding system contract. Do not edit manually.
 */
library ForceFieldProgramLib {
  error ForceFieldProgramLib_CallingFromRootSystem();

  function validateProgram(
    ForceFieldProgramType self,
    HookContext memory ctx,
    IProgramValidator.ProgramData memory __auxArg0
  ) internal view {
    return CallWrapper(self.toResourceId(), address(0)).validateProgram(ctx, __auxArg0);
  }

  function onEnergize(
    ForceFieldProgramType self,
    HookContext memory ctx,
    IEnergize.EnergizeData memory energize
  ) internal {
    return CallWrapper(self.toResourceId(), address(0)).onEnergize(ctx, energize);
  }

  function onHit(ForceFieldProgramType self, HookContext memory ctx, IHit.HitData memory hit) internal {
    return CallWrapper(self.toResourceId(), address(0)).onHit(ctx, hit);
  }

  function onAddFragment(
    ForceFieldProgramType self,
    HookContext memory ctx,
    IAddFragment.AddFragmentData memory fragment
  ) internal view {
    return CallWrapper(self.toResourceId(), address(0)).onAddFragment(ctx, fragment);
  }

  function onRemoveFragment(
    ForceFieldProgramType self,
    HookContext memory ctx,
    IRemoveFragment.RemoveFragmentData memory __auxArg0
  ) internal view {
    return CallWrapper(self.toResourceId(), address(0)).onRemoveFragment(ctx, __auxArg0);
  }

  function onBuild(ForceFieldProgramType self, HookContext memory ctx, IBuild.BuildData memory build) internal {
    return CallWrapper(self.toResourceId(), address(0)).onBuild(ctx, build);
  }

  function onMine(ForceFieldProgramType self, HookContext memory ctx, IMine.MineData memory mine) internal view {
    return CallWrapper(self.toResourceId(), address(0)).onMine(ctx, mine);
  }

  function _msgSender(ForceFieldProgramType self) internal view returns (address __auxRet0) {
    return CallWrapper(self.toResourceId(), address(0))._msgSender();
  }

  function _msgValue(ForceFieldProgramType self) internal view returns (uint256 __auxRet0) {
    return CallWrapper(self.toResourceId(), address(0))._msgValue();
  }

  function validateProgram(
    CallWrapper memory self,
    HookContext memory ctx,
    IProgramValidator.ProgramData memory __auxArg0
  ) internal view {
    // if the contract calling this function is a root system, it should use `callAsRoot`
    if (address(_world()) == address(this)) revert ForceFieldProgramLib_CallingFromRootSystem();

    bytes memory systemCall = abi.encodeCall(
      _validateProgram_HookContext_IProgramValidator_ProgramData.validateProgram,
      (ctx, __auxArg0)
    );
    bytes memory worldCall = self.from == address(0)
      ? abi.encodeCall(IWorldCall.call, (self.systemId, systemCall))
      : abi.encodeCall(IWorldCall.callFrom, (self.from, self.systemId, systemCall));
    (bool success, bytes memory returnData) = address(_world()).staticcall(worldCall);
    if (!success) revertWithBytes(returnData);
    abi.decode(returnData, (bytes));
  }

  function onEnergize(
    CallWrapper memory self,
    HookContext memory ctx,
    IEnergize.EnergizeData memory energize
  ) internal {
    // if the contract calling this function is a root system, it should use `callAsRoot`
    if (address(_world()) == address(this)) revert ForceFieldProgramLib_CallingFromRootSystem();

    bytes memory systemCall = abi.encodeCall(
      _onEnergize_HookContext_IEnergize_EnergizeData.onEnergize,
      (ctx, energize)
    );
    self.from == address(0)
      ? _world().call(self.systemId, systemCall)
      : _world().callFrom(self.from, self.systemId, systemCall);
  }

  function onHit(CallWrapper memory self, HookContext memory ctx, IHit.HitData memory hit) internal {
    // if the contract calling this function is a root system, it should use `callAsRoot`
    if (address(_world()) == address(this)) revert ForceFieldProgramLib_CallingFromRootSystem();

    bytes memory systemCall = abi.encodeCall(_onHit_HookContext_IHit_HitData.onHit, (ctx, hit));
    self.from == address(0)
      ? _world().call(self.systemId, systemCall)
      : _world().callFrom(self.from, self.systemId, systemCall);
  }

  function onAddFragment(
    CallWrapper memory self,
    HookContext memory ctx,
    IAddFragment.AddFragmentData memory fragment
  ) internal view {
    // if the contract calling this function is a root system, it should use `callAsRoot`
    if (address(_world()) == address(this)) revert ForceFieldProgramLib_CallingFromRootSystem();

    bytes memory systemCall = abi.encodeCall(
      _onAddFragment_HookContext_IAddFragment_AddFragmentData.onAddFragment,
      (ctx, fragment)
    );
    bytes memory worldCall = self.from == address(0)
      ? abi.encodeCall(IWorldCall.call, (self.systemId, systemCall))
      : abi.encodeCall(IWorldCall.callFrom, (self.from, self.systemId, systemCall));
    (bool success, bytes memory returnData) = address(_world()).staticcall(worldCall);
    if (!success) revertWithBytes(returnData);
    abi.decode(returnData, (bytes));
  }

  function onRemoveFragment(
    CallWrapper memory self,
    HookContext memory ctx,
    IRemoveFragment.RemoveFragmentData memory __auxArg0
  ) internal view {
    // if the contract calling this function is a root system, it should use `callAsRoot`
    if (address(_world()) == address(this)) revert ForceFieldProgramLib_CallingFromRootSystem();

    bytes memory systemCall = abi.encodeCall(
      _onRemoveFragment_HookContext_IRemoveFragment_RemoveFragmentData.onRemoveFragment,
      (ctx, __auxArg0)
    );
    bytes memory worldCall = self.from == address(0)
      ? abi.encodeCall(IWorldCall.call, (self.systemId, systemCall))
      : abi.encodeCall(IWorldCall.callFrom, (self.from, self.systemId, systemCall));
    (bool success, bytes memory returnData) = address(_world()).staticcall(worldCall);
    if (!success) revertWithBytes(returnData);
    abi.decode(returnData, (bytes));
  }

  function onBuild(CallWrapper memory self, HookContext memory ctx, IBuild.BuildData memory build) internal {
    // if the contract calling this function is a root system, it should use `callAsRoot`
    if (address(_world()) == address(this)) revert ForceFieldProgramLib_CallingFromRootSystem();

    bytes memory systemCall = abi.encodeCall(_onBuild_HookContext_IBuild_BuildData.onBuild, (ctx, build));
    self.from == address(0)
      ? _world().call(self.systemId, systemCall)
      : _world().callFrom(self.from, self.systemId, systemCall);
  }

  function onMine(CallWrapper memory self, HookContext memory ctx, IMine.MineData memory mine) internal view {
    // if the contract calling this function is a root system, it should use `callAsRoot`
    if (address(_world()) == address(this)) revert ForceFieldProgramLib_CallingFromRootSystem();

    bytes memory systemCall = abi.encodeCall(_onMine_HookContext_IMine_MineData.onMine, (ctx, mine));
    bytes memory worldCall = self.from == address(0)
      ? abi.encodeCall(IWorldCall.call, (self.systemId, systemCall))
      : abi.encodeCall(IWorldCall.callFrom, (self.from, self.systemId, systemCall));
    (bool success, bytes memory returnData) = address(_world()).staticcall(worldCall);
    if (!success) revertWithBytes(returnData);
    abi.decode(returnData, (bytes));
  }

  function _msgSender(CallWrapper memory self) internal view returns (address __auxRet0) {
    // if the contract calling this function is a root system, it should use `callAsRoot`
    if (address(_world()) == address(this)) revert ForceFieldProgramLib_CallingFromRootSystem();

    bytes memory systemCall = abi.encodeCall(__msgSender._msgSender, ());
    bytes memory worldCall = self.from == address(0)
      ? abi.encodeCall(IWorldCall.call, (self.systemId, systemCall))
      : abi.encodeCall(IWorldCall.callFrom, (self.from, self.systemId, systemCall));
    (bool success, bytes memory returnData) = address(_world()).staticcall(worldCall);
    if (!success) revertWithBytes(returnData);

    bytes memory result = abi.decode(returnData, (bytes));
    // skip decoding an empty result, which can happen after expectRevert
    if (result.length != 0) {
      return abi.decode(result, (address));
    }
  }

  function _msgValue(CallWrapper memory self) internal view returns (uint256 __auxRet0) {
    // if the contract calling this function is a root system, it should use `callAsRoot`
    if (address(_world()) == address(this)) revert ForceFieldProgramLib_CallingFromRootSystem();

    bytes memory systemCall = abi.encodeCall(__msgValue._msgValue, ());
    bytes memory worldCall = self.from == address(0)
      ? abi.encodeCall(IWorldCall.call, (self.systemId, systemCall))
      : abi.encodeCall(IWorldCall.callFrom, (self.from, self.systemId, systemCall));
    (bool success, bytes memory returnData) = address(_world()).staticcall(worldCall);
    if (!success) revertWithBytes(returnData);

    bytes memory result = abi.decode(returnData, (bytes));
    // skip decoding an empty result, which can happen after expectRevert
    if (result.length != 0) {
      return abi.decode(result, (uint256));
    }
  }

  function validateProgram(
    RootCallWrapper memory self,
    HookContext memory ctx,
    IProgramValidator.ProgramData memory __auxArg0
  ) internal view {
    bytes memory systemCall = abi.encodeCall(
      _validateProgram_HookContext_IProgramValidator_ProgramData.validateProgram,
      (ctx, __auxArg0)
    );
    SystemCall.staticcallOrRevert(self.from, self.systemId, systemCall);
  }

  function onEnergize(
    RootCallWrapper memory self,
    HookContext memory ctx,
    IEnergize.EnergizeData memory energize
  ) internal {
    bytes memory systemCall = abi.encodeCall(
      _onEnergize_HookContext_IEnergize_EnergizeData.onEnergize,
      (ctx, energize)
    );
    SystemCall.callWithHooksOrRevert(self.from, self.systemId, systemCall, msg.value);
  }

  function onHit(RootCallWrapper memory self, HookContext memory ctx, IHit.HitData memory hit) internal {
    bytes memory systemCall = abi.encodeCall(_onHit_HookContext_IHit_HitData.onHit, (ctx, hit));
    SystemCall.callWithHooksOrRevert(self.from, self.systemId, systemCall, msg.value);
  }

  function onAddFragment(
    RootCallWrapper memory self,
    HookContext memory ctx,
    IAddFragment.AddFragmentData memory fragment
  ) internal view {
    bytes memory systemCall = abi.encodeCall(
      _onAddFragment_HookContext_IAddFragment_AddFragmentData.onAddFragment,
      (ctx, fragment)
    );
    SystemCall.staticcallOrRevert(self.from, self.systemId, systemCall);
  }

  function onRemoveFragment(
    RootCallWrapper memory self,
    HookContext memory ctx,
    IRemoveFragment.RemoveFragmentData memory __auxArg0
  ) internal view {
    bytes memory systemCall = abi.encodeCall(
      _onRemoveFragment_HookContext_IRemoveFragment_RemoveFragmentData.onRemoveFragment,
      (ctx, __auxArg0)
    );
    SystemCall.staticcallOrRevert(self.from, self.systemId, systemCall);
  }

  function onBuild(RootCallWrapper memory self, HookContext memory ctx, IBuild.BuildData memory build) internal {
    bytes memory systemCall = abi.encodeCall(_onBuild_HookContext_IBuild_BuildData.onBuild, (ctx, build));
    SystemCall.callWithHooksOrRevert(self.from, self.systemId, systemCall, msg.value);
  }

  function onMine(RootCallWrapper memory self, HookContext memory ctx, IMine.MineData memory mine) internal view {
    bytes memory systemCall = abi.encodeCall(_onMine_HookContext_IMine_MineData.onMine, (ctx, mine));
    SystemCall.staticcallOrRevert(self.from, self.systemId, systemCall);
  }

  function _msgSender(RootCallWrapper memory self) internal view returns (address __auxRet0) {
    bytes memory systemCall = abi.encodeCall(__msgSender._msgSender, ());

    bytes memory result = SystemCall.staticcallOrRevert(self.from, self.systemId, systemCall);
    // skip decoding an empty result, which can happen after expectRevert
    if (result.length != 0) {
      return abi.decode(result, (address));
    }
  }

  function _msgValue(RootCallWrapper memory self) internal view returns (uint256 __auxRet0) {
    bytes memory systemCall = abi.encodeCall(__msgValue._msgValue, ());

    bytes memory result = SystemCall.staticcallOrRevert(self.from, self.systemId, systemCall);
    // skip decoding an empty result, which can happen after expectRevert
    if (result.length != 0) {
      return abi.decode(result, (uint256));
    }
  }

  function callFrom(ForceFieldProgramType self, address from) internal pure returns (CallWrapper memory) {
    return CallWrapper(self.toResourceId(), from);
  }

  function callAsRoot(ForceFieldProgramType self) internal view returns (RootCallWrapper memory) {
    return RootCallWrapper(self.toResourceId(), WorldContextConsumerLib._msgSender());
  }

  function callAsRootFrom(ForceFieldProgramType self, address from) internal pure returns (RootCallWrapper memory) {
    return RootCallWrapper(self.toResourceId(), from);
  }

  function toResourceId(ForceFieldProgramType self) internal pure returns (ResourceId) {
    return ResourceId.wrap(ForceFieldProgramType.unwrap(self));
  }

  function fromResourceId(ResourceId resourceId) internal pure returns (ForceFieldProgramType) {
    return ForceFieldProgramType.wrap(resourceId.unwrap());
  }

  function getAddress(ForceFieldProgramType self) internal view returns (address) {
    return Systems.getSystem(self.toResourceId());
  }

  function _world() private view returns (IWorldCall) {
    return IWorldCall(StoreSwitch.getStoreAddress());
  }
}

/**
 * System Function Interfaces
 *
 * We generate an interface for each system function, which is then used for encoding system calls.
 * This is necessary to handle function overloading correctly (which abi.encodeCall cannot).
 *
 * Each interface is uniquely named based on the function name and parameters to prevent collisions.
 */

interface _validateProgram_HookContext_IProgramValidator_ProgramData {
  function validateProgram(HookContext memory ctx, IProgramValidator.ProgramData memory __auxArg0) external;
}

interface _onEnergize_HookContext_IEnergize_EnergizeData {
  function onEnergize(HookContext memory ctx, IEnergize.EnergizeData memory energize) external;
}

interface _onHit_HookContext_IHit_HitData {
  function onHit(HookContext memory ctx, IHit.HitData memory hit) external;
}

interface _onAddFragment_HookContext_IAddFragment_AddFragmentData {
  function onAddFragment(HookContext memory ctx, IAddFragment.AddFragmentData memory fragment) external;
}

interface _onRemoveFragment_HookContext_IRemoveFragment_RemoveFragmentData {
  function onRemoveFragment(HookContext memory ctx, IRemoveFragment.RemoveFragmentData memory __auxArg0) external;
}

interface _onBuild_HookContext_IBuild_BuildData {
  function onBuild(HookContext memory ctx, IBuild.BuildData memory build) external;
}

interface _onMine_HookContext_IMine_MineData {
  function onMine(HookContext memory ctx, IMine.MineData memory mine) external;
}

interface __msgSender {
  function _msgSender() external;
}

interface __msgValue {
  function _msgValue() external;
}

using ForceFieldProgramLib for ForceFieldProgramType global;
using ForceFieldProgramLib for CallWrapper global;
using ForceFieldProgramLib for RootCallWrapper global;
