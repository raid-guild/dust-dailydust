// SPDX-License-Identifier: MIT
pragma solidity >=0.8.24;

/* Autogenerated file. Do not edit manually. */

// Import store internals
import { IStore } from "@latticexyz/store/src/IStore.sol";
import { StoreSwitch } from "@latticexyz/store/src/StoreSwitch.sol";
import { StoreCore } from "@latticexyz/store/src/StoreCore.sol";
import { Bytes } from "@latticexyz/store/src/Bytes.sol";
import { Memory } from "@latticexyz/store/src/Memory.sol";
import { SliceLib } from "@latticexyz/store/src/Slice.sol";
import { EncodeArray } from "@latticexyz/store/src/tightcoder/EncodeArray.sol";
import { FieldLayout } from "@latticexyz/store/src/FieldLayout.sol";
import { Schema } from "@latticexyz/store/src/Schema.sol";
import { EncodedLengths, EncodedLengthsLib } from "@latticexyz/store/src/EncodedLengths.sol";
import { ResourceId } from "@latticexyz/store/src/ResourceId.sol";

struct WaypointStepData {
  int32 x;
  int32 y;
  int32 z;
  string label;
}

library WaypointStep {
  // Hex below is the result of `WorldResourceIdLib.encode({ namespace: "rg_dd_0001", name: "WaypointStep", typeId: RESOURCE_TABLE });`
  ResourceId constant _tableId = ResourceId.wrap(0x746272675f64645f3030303100000000576179706f696e745374657000000000);

  FieldLayout constant _fieldLayout =
    FieldLayout.wrap(0x000c030104040400000000000000000000000000000000000000000000000000);

  // Hex-encoded key schema of (bytes32, uint16, uint16)
  Schema constant _keySchema = Schema.wrap(0x002403005f010100000000000000000000000000000000000000000000000000);
  // Hex-encoded value schema of (int32, int32, int32, string)
  Schema constant _valueSchema = Schema.wrap(0x000c0301232323c5000000000000000000000000000000000000000000000000);

  /**
   * @notice Get the table's key field names.
   * @return keyNames An array of strings with the names of key fields.
   */
  function getKeyNames() internal pure returns (string[] memory keyNames) {
    keyNames = new string[](3);
    keyNames[0] = "noteId";
    keyNames[1] = "groupId";
    keyNames[2] = "index";
  }

  /**
   * @notice Get the table's value field names.
   * @return fieldNames An array of strings with the names of value fields.
   */
  function getFieldNames() internal pure returns (string[] memory fieldNames) {
    fieldNames = new string[](4);
    fieldNames[0] = "x";
    fieldNames[1] = "y";
    fieldNames[2] = "z";
    fieldNames[3] = "label";
  }

  /**
   * @notice Register the table with its config.
   */
  function register() internal {
    StoreSwitch.registerTable(_tableId, _fieldLayout, _keySchema, _valueSchema, getKeyNames(), getFieldNames());
  }

  /**
   * @notice Register the table with its config.
   */
  function _register() internal {
    StoreCore.registerTable(_tableId, _fieldLayout, _keySchema, _valueSchema, getKeyNames(), getFieldNames());
  }

  /**
   * @notice Get x.
   */
  function getX(bytes32 noteId, uint16 groupId, uint16 index) internal view returns (int32 x) {
    bytes32[] memory _keyTuple = new bytes32[](3);
    _keyTuple[0] = noteId;
    _keyTuple[1] = bytes32(uint256(groupId));
    _keyTuple[2] = bytes32(uint256(index));

    bytes32 _blob = StoreSwitch.getStaticField(_tableId, _keyTuple, 0, _fieldLayout);
    return (int32(uint32(bytes4(_blob))));
  }

  /**
   * @notice Get x.
   */
  function _getX(bytes32 noteId, uint16 groupId, uint16 index) internal view returns (int32 x) {
    bytes32[] memory _keyTuple = new bytes32[](3);
    _keyTuple[0] = noteId;
    _keyTuple[1] = bytes32(uint256(groupId));
    _keyTuple[2] = bytes32(uint256(index));

    bytes32 _blob = StoreCore.getStaticField(_tableId, _keyTuple, 0, _fieldLayout);
    return (int32(uint32(bytes4(_blob))));
  }

  /**
   * @notice Set x.
   */
  function setX(bytes32 noteId, uint16 groupId, uint16 index, int32 x) internal {
    bytes32[] memory _keyTuple = new bytes32[](3);
    _keyTuple[0] = noteId;
    _keyTuple[1] = bytes32(uint256(groupId));
    _keyTuple[2] = bytes32(uint256(index));

    StoreSwitch.setStaticField(_tableId, _keyTuple, 0, abi.encodePacked((x)), _fieldLayout);
  }

  /**
   * @notice Set x.
   */
  function _setX(bytes32 noteId, uint16 groupId, uint16 index, int32 x) internal {
    bytes32[] memory _keyTuple = new bytes32[](3);
    _keyTuple[0] = noteId;
    _keyTuple[1] = bytes32(uint256(groupId));
    _keyTuple[2] = bytes32(uint256(index));

    StoreCore.setStaticField(_tableId, _keyTuple, 0, abi.encodePacked((x)), _fieldLayout);
  }

  /**
   * @notice Get y.
   */
  function getY(bytes32 noteId, uint16 groupId, uint16 index) internal view returns (int32 y) {
    bytes32[] memory _keyTuple = new bytes32[](3);
    _keyTuple[0] = noteId;
    _keyTuple[1] = bytes32(uint256(groupId));
    _keyTuple[2] = bytes32(uint256(index));

    bytes32 _blob = StoreSwitch.getStaticField(_tableId, _keyTuple, 1, _fieldLayout);
    return (int32(uint32(bytes4(_blob))));
  }

  /**
   * @notice Get y.
   */
  function _getY(bytes32 noteId, uint16 groupId, uint16 index) internal view returns (int32 y) {
    bytes32[] memory _keyTuple = new bytes32[](3);
    _keyTuple[0] = noteId;
    _keyTuple[1] = bytes32(uint256(groupId));
    _keyTuple[2] = bytes32(uint256(index));

    bytes32 _blob = StoreCore.getStaticField(_tableId, _keyTuple, 1, _fieldLayout);
    return (int32(uint32(bytes4(_blob))));
  }

  /**
   * @notice Set y.
   */
  function setY(bytes32 noteId, uint16 groupId, uint16 index, int32 y) internal {
    bytes32[] memory _keyTuple = new bytes32[](3);
    _keyTuple[0] = noteId;
    _keyTuple[1] = bytes32(uint256(groupId));
    _keyTuple[2] = bytes32(uint256(index));

    StoreSwitch.setStaticField(_tableId, _keyTuple, 1, abi.encodePacked((y)), _fieldLayout);
  }

  /**
   * @notice Set y.
   */
  function _setY(bytes32 noteId, uint16 groupId, uint16 index, int32 y) internal {
    bytes32[] memory _keyTuple = new bytes32[](3);
    _keyTuple[0] = noteId;
    _keyTuple[1] = bytes32(uint256(groupId));
    _keyTuple[2] = bytes32(uint256(index));

    StoreCore.setStaticField(_tableId, _keyTuple, 1, abi.encodePacked((y)), _fieldLayout);
  }

  /**
   * @notice Get z.
   */
  function getZ(bytes32 noteId, uint16 groupId, uint16 index) internal view returns (int32 z) {
    bytes32[] memory _keyTuple = new bytes32[](3);
    _keyTuple[0] = noteId;
    _keyTuple[1] = bytes32(uint256(groupId));
    _keyTuple[2] = bytes32(uint256(index));

    bytes32 _blob = StoreSwitch.getStaticField(_tableId, _keyTuple, 2, _fieldLayout);
    return (int32(uint32(bytes4(_blob))));
  }

  /**
   * @notice Get z.
   */
  function _getZ(bytes32 noteId, uint16 groupId, uint16 index) internal view returns (int32 z) {
    bytes32[] memory _keyTuple = new bytes32[](3);
    _keyTuple[0] = noteId;
    _keyTuple[1] = bytes32(uint256(groupId));
    _keyTuple[2] = bytes32(uint256(index));

    bytes32 _blob = StoreCore.getStaticField(_tableId, _keyTuple, 2, _fieldLayout);
    return (int32(uint32(bytes4(_blob))));
  }

  /**
   * @notice Set z.
   */
  function setZ(bytes32 noteId, uint16 groupId, uint16 index, int32 z) internal {
    bytes32[] memory _keyTuple = new bytes32[](3);
    _keyTuple[0] = noteId;
    _keyTuple[1] = bytes32(uint256(groupId));
    _keyTuple[2] = bytes32(uint256(index));

    StoreSwitch.setStaticField(_tableId, _keyTuple, 2, abi.encodePacked((z)), _fieldLayout);
  }

  /**
   * @notice Set z.
   */
  function _setZ(bytes32 noteId, uint16 groupId, uint16 index, int32 z) internal {
    bytes32[] memory _keyTuple = new bytes32[](3);
    _keyTuple[0] = noteId;
    _keyTuple[1] = bytes32(uint256(groupId));
    _keyTuple[2] = bytes32(uint256(index));

    StoreCore.setStaticField(_tableId, _keyTuple, 2, abi.encodePacked((z)), _fieldLayout);
  }

  /**
   * @notice Get label.
   */
  function getLabel(bytes32 noteId, uint16 groupId, uint16 index) internal view returns (string memory label) {
    bytes32[] memory _keyTuple = new bytes32[](3);
    _keyTuple[0] = noteId;
    _keyTuple[1] = bytes32(uint256(groupId));
    _keyTuple[2] = bytes32(uint256(index));

    bytes memory _blob = StoreSwitch.getDynamicField(_tableId, _keyTuple, 0);
    return (string(_blob));
  }

  /**
   * @notice Get label.
   */
  function _getLabel(bytes32 noteId, uint16 groupId, uint16 index) internal view returns (string memory label) {
    bytes32[] memory _keyTuple = new bytes32[](3);
    _keyTuple[0] = noteId;
    _keyTuple[1] = bytes32(uint256(groupId));
    _keyTuple[2] = bytes32(uint256(index));

    bytes memory _blob = StoreCore.getDynamicField(_tableId, _keyTuple, 0);
    return (string(_blob));
  }

  /**
   * @notice Set label.
   */
  function setLabel(bytes32 noteId, uint16 groupId, uint16 index, string memory label) internal {
    bytes32[] memory _keyTuple = new bytes32[](3);
    _keyTuple[0] = noteId;
    _keyTuple[1] = bytes32(uint256(groupId));
    _keyTuple[2] = bytes32(uint256(index));

    StoreSwitch.setDynamicField(_tableId, _keyTuple, 0, bytes((label)));
  }

  /**
   * @notice Set label.
   */
  function _setLabel(bytes32 noteId, uint16 groupId, uint16 index, string memory label) internal {
    bytes32[] memory _keyTuple = new bytes32[](3);
    _keyTuple[0] = noteId;
    _keyTuple[1] = bytes32(uint256(groupId));
    _keyTuple[2] = bytes32(uint256(index));

    StoreCore.setDynamicField(_tableId, _keyTuple, 0, bytes((label)));
  }

  /**
   * @notice Get the length of label.
   */
  function lengthLabel(bytes32 noteId, uint16 groupId, uint16 index) internal view returns (uint256) {
    bytes32[] memory _keyTuple = new bytes32[](3);
    _keyTuple[0] = noteId;
    _keyTuple[1] = bytes32(uint256(groupId));
    _keyTuple[2] = bytes32(uint256(index));

    uint256 _byteLength = StoreSwitch.getDynamicFieldLength(_tableId, _keyTuple, 0);
    unchecked {
      return _byteLength / 1;
    }
  }

  /**
   * @notice Get the length of label.
   */
  function _lengthLabel(bytes32 noteId, uint16 groupId, uint16 index) internal view returns (uint256) {
    bytes32[] memory _keyTuple = new bytes32[](3);
    _keyTuple[0] = noteId;
    _keyTuple[1] = bytes32(uint256(groupId));
    _keyTuple[2] = bytes32(uint256(index));

    uint256 _byteLength = StoreCore.getDynamicFieldLength(_tableId, _keyTuple, 0);
    unchecked {
      return _byteLength / 1;
    }
  }

  /**
   * @notice Get an item of label.
   * @dev Reverts with Store_IndexOutOfBounds if `_index` is out of bounds for the array.
   */
  function getItemLabel(
    bytes32 noteId,
    uint16 groupId,
    uint16 index,
    uint256 _index
  ) internal view returns (string memory) {
    bytes32[] memory _keyTuple = new bytes32[](3);
    _keyTuple[0] = noteId;
    _keyTuple[1] = bytes32(uint256(groupId));
    _keyTuple[2] = bytes32(uint256(index));

    unchecked {
      bytes memory _blob = StoreSwitch.getDynamicFieldSlice(_tableId, _keyTuple, 0, _index * 1, (_index + 1) * 1);
      return (string(_blob));
    }
  }

  /**
   * @notice Get an item of label.
   * @dev Reverts with Store_IndexOutOfBounds if `_index` is out of bounds for the array.
   */
  function _getItemLabel(
    bytes32 noteId,
    uint16 groupId,
    uint16 index,
    uint256 _index
  ) internal view returns (string memory) {
    bytes32[] memory _keyTuple = new bytes32[](3);
    _keyTuple[0] = noteId;
    _keyTuple[1] = bytes32(uint256(groupId));
    _keyTuple[2] = bytes32(uint256(index));

    unchecked {
      bytes memory _blob = StoreCore.getDynamicFieldSlice(_tableId, _keyTuple, 0, _index * 1, (_index + 1) * 1);
      return (string(_blob));
    }
  }

  /**
   * @notice Push a slice to label.
   */
  function pushLabel(bytes32 noteId, uint16 groupId, uint16 index, string memory _slice) internal {
    bytes32[] memory _keyTuple = new bytes32[](3);
    _keyTuple[0] = noteId;
    _keyTuple[1] = bytes32(uint256(groupId));
    _keyTuple[2] = bytes32(uint256(index));

    StoreSwitch.pushToDynamicField(_tableId, _keyTuple, 0, bytes((_slice)));
  }

  /**
   * @notice Push a slice to label.
   */
  function _pushLabel(bytes32 noteId, uint16 groupId, uint16 index, string memory _slice) internal {
    bytes32[] memory _keyTuple = new bytes32[](3);
    _keyTuple[0] = noteId;
    _keyTuple[1] = bytes32(uint256(groupId));
    _keyTuple[2] = bytes32(uint256(index));

    StoreCore.pushToDynamicField(_tableId, _keyTuple, 0, bytes((_slice)));
  }

  /**
   * @notice Pop a slice from label.
   */
  function popLabel(bytes32 noteId, uint16 groupId, uint16 index) internal {
    bytes32[] memory _keyTuple = new bytes32[](3);
    _keyTuple[0] = noteId;
    _keyTuple[1] = bytes32(uint256(groupId));
    _keyTuple[2] = bytes32(uint256(index));

    StoreSwitch.popFromDynamicField(_tableId, _keyTuple, 0, 1);
  }

  /**
   * @notice Pop a slice from label.
   */
  function _popLabel(bytes32 noteId, uint16 groupId, uint16 index) internal {
    bytes32[] memory _keyTuple = new bytes32[](3);
    _keyTuple[0] = noteId;
    _keyTuple[1] = bytes32(uint256(groupId));
    _keyTuple[2] = bytes32(uint256(index));

    StoreCore.popFromDynamicField(_tableId, _keyTuple, 0, 1);
  }

  /**
   * @notice Update a slice of label at `_index`.
   */
  function updateLabel(bytes32 noteId, uint16 groupId, uint16 index, uint256 _index, string memory _slice) internal {
    bytes32[] memory _keyTuple = new bytes32[](3);
    _keyTuple[0] = noteId;
    _keyTuple[1] = bytes32(uint256(groupId));
    _keyTuple[2] = bytes32(uint256(index));

    unchecked {
      bytes memory _encoded = bytes((_slice));
      StoreSwitch.spliceDynamicData(_tableId, _keyTuple, 0, uint40(_index * 1), uint40(_encoded.length), _encoded);
    }
  }

  /**
   * @notice Update a slice of label at `_index`.
   */
  function _updateLabel(bytes32 noteId, uint16 groupId, uint16 index, uint256 _index, string memory _slice) internal {
    bytes32[] memory _keyTuple = new bytes32[](3);
    _keyTuple[0] = noteId;
    _keyTuple[1] = bytes32(uint256(groupId));
    _keyTuple[2] = bytes32(uint256(index));

    unchecked {
      bytes memory _encoded = bytes((_slice));
      StoreCore.spliceDynamicData(_tableId, _keyTuple, 0, uint40(_index * 1), uint40(_encoded.length), _encoded);
    }
  }

  /**
   * @notice Get the full data.
   */
  function get(bytes32 noteId, uint16 groupId, uint16 index) internal view returns (WaypointStepData memory _table) {
    bytes32[] memory _keyTuple = new bytes32[](3);
    _keyTuple[0] = noteId;
    _keyTuple[1] = bytes32(uint256(groupId));
    _keyTuple[2] = bytes32(uint256(index));

    (bytes memory _staticData, EncodedLengths _encodedLengths, bytes memory _dynamicData) = StoreSwitch.getRecord(
      _tableId,
      _keyTuple,
      _fieldLayout
    );
    return decode(_staticData, _encodedLengths, _dynamicData);
  }

  /**
   * @notice Get the full data.
   */
  function _get(bytes32 noteId, uint16 groupId, uint16 index) internal view returns (WaypointStepData memory _table) {
    bytes32[] memory _keyTuple = new bytes32[](3);
    _keyTuple[0] = noteId;
    _keyTuple[1] = bytes32(uint256(groupId));
    _keyTuple[2] = bytes32(uint256(index));

    (bytes memory _staticData, EncodedLengths _encodedLengths, bytes memory _dynamicData) = StoreCore.getRecord(
      _tableId,
      _keyTuple,
      _fieldLayout
    );
    return decode(_staticData, _encodedLengths, _dynamicData);
  }

  /**
   * @notice Set the full data using individual values.
   */
  function set(bytes32 noteId, uint16 groupId, uint16 index, int32 x, int32 y, int32 z, string memory label) internal {
    bytes memory _staticData = encodeStatic(x, y, z);

    EncodedLengths _encodedLengths = encodeLengths(label);
    bytes memory _dynamicData = encodeDynamic(label);

    bytes32[] memory _keyTuple = new bytes32[](3);
    _keyTuple[0] = noteId;
    _keyTuple[1] = bytes32(uint256(groupId));
    _keyTuple[2] = bytes32(uint256(index));

    StoreSwitch.setRecord(_tableId, _keyTuple, _staticData, _encodedLengths, _dynamicData);
  }

  /**
   * @notice Set the full data using individual values.
   */
  function _set(bytes32 noteId, uint16 groupId, uint16 index, int32 x, int32 y, int32 z, string memory label) internal {
    bytes memory _staticData = encodeStatic(x, y, z);

    EncodedLengths _encodedLengths = encodeLengths(label);
    bytes memory _dynamicData = encodeDynamic(label);

    bytes32[] memory _keyTuple = new bytes32[](3);
    _keyTuple[0] = noteId;
    _keyTuple[1] = bytes32(uint256(groupId));
    _keyTuple[2] = bytes32(uint256(index));

    StoreCore.setRecord(_tableId, _keyTuple, _staticData, _encodedLengths, _dynamicData, _fieldLayout);
  }

  /**
   * @notice Set the full data using the data struct.
   */
  function set(bytes32 noteId, uint16 groupId, uint16 index, WaypointStepData memory _table) internal {
    bytes memory _staticData = encodeStatic(_table.x, _table.y, _table.z);

    EncodedLengths _encodedLengths = encodeLengths(_table.label);
    bytes memory _dynamicData = encodeDynamic(_table.label);

    bytes32[] memory _keyTuple = new bytes32[](3);
    _keyTuple[0] = noteId;
    _keyTuple[1] = bytes32(uint256(groupId));
    _keyTuple[2] = bytes32(uint256(index));

    StoreSwitch.setRecord(_tableId, _keyTuple, _staticData, _encodedLengths, _dynamicData);
  }

  /**
   * @notice Set the full data using the data struct.
   */
  function _set(bytes32 noteId, uint16 groupId, uint16 index, WaypointStepData memory _table) internal {
    bytes memory _staticData = encodeStatic(_table.x, _table.y, _table.z);

    EncodedLengths _encodedLengths = encodeLengths(_table.label);
    bytes memory _dynamicData = encodeDynamic(_table.label);

    bytes32[] memory _keyTuple = new bytes32[](3);
    _keyTuple[0] = noteId;
    _keyTuple[1] = bytes32(uint256(groupId));
    _keyTuple[2] = bytes32(uint256(index));

    StoreCore.setRecord(_tableId, _keyTuple, _staticData, _encodedLengths, _dynamicData, _fieldLayout);
  }

  /**
   * @notice Decode the tightly packed blob of static data using this table's field layout.
   */
  function decodeStatic(bytes memory _blob) internal pure returns (int32 x, int32 y, int32 z) {
    x = (int32(uint32(Bytes.getBytes4(_blob, 0))));

    y = (int32(uint32(Bytes.getBytes4(_blob, 4))));

    z = (int32(uint32(Bytes.getBytes4(_blob, 8))));
  }

  /**
   * @notice Decode the tightly packed blob of dynamic data using the encoded lengths.
   */
  function decodeDynamic(
    EncodedLengths _encodedLengths,
    bytes memory _blob
  ) internal pure returns (string memory label) {
    uint256 _start;
    uint256 _end;
    unchecked {
      _end = _encodedLengths.atIndex(0);
    }
    label = (string(SliceLib.getSubslice(_blob, _start, _end).toBytes()));
  }

  /**
   * @notice Decode the tightly packed blobs using this table's field layout.
   * @param _staticData Tightly packed static fields.
   * @param _encodedLengths Encoded lengths of dynamic fields.
   * @param _dynamicData Tightly packed dynamic fields.
   */
  function decode(
    bytes memory _staticData,
    EncodedLengths _encodedLengths,
    bytes memory _dynamicData
  ) internal pure returns (WaypointStepData memory _table) {
    (_table.x, _table.y, _table.z) = decodeStatic(_staticData);

    (_table.label) = decodeDynamic(_encodedLengths, _dynamicData);
  }

  /**
   * @notice Delete all data for given keys.
   */
  function deleteRecord(bytes32 noteId, uint16 groupId, uint16 index) internal {
    bytes32[] memory _keyTuple = new bytes32[](3);
    _keyTuple[0] = noteId;
    _keyTuple[1] = bytes32(uint256(groupId));
    _keyTuple[2] = bytes32(uint256(index));

    StoreSwitch.deleteRecord(_tableId, _keyTuple);
  }

  /**
   * @notice Delete all data for given keys.
   */
  function _deleteRecord(bytes32 noteId, uint16 groupId, uint16 index) internal {
    bytes32[] memory _keyTuple = new bytes32[](3);
    _keyTuple[0] = noteId;
    _keyTuple[1] = bytes32(uint256(groupId));
    _keyTuple[2] = bytes32(uint256(index));

    StoreCore.deleteRecord(_tableId, _keyTuple, _fieldLayout);
  }

  /**
   * @notice Tightly pack static (fixed length) data using this table's schema.
   * @return The static data, encoded into a sequence of bytes.
   */
  function encodeStatic(int32 x, int32 y, int32 z) internal pure returns (bytes memory) {
    return abi.encodePacked(x, y, z);
  }

  /**
   * @notice Tightly pack dynamic data lengths using this table's schema.
   * @return _encodedLengths The lengths of the dynamic fields (packed into a single bytes32 value).
   */
  function encodeLengths(string memory label) internal pure returns (EncodedLengths _encodedLengths) {
    // Lengths are effectively checked during copy by 2**40 bytes exceeding gas limits
    unchecked {
      _encodedLengths = EncodedLengthsLib.pack(bytes(label).length);
    }
  }

  /**
   * @notice Tightly pack dynamic (variable length) data using this table's schema.
   * @return The dynamic data, encoded into a sequence of bytes.
   */
  function encodeDynamic(string memory label) internal pure returns (bytes memory) {
    return abi.encodePacked(bytes((label)));
  }

  /**
   * @notice Encode all of a record's fields.
   * @return The static (fixed length) data, encoded into a sequence of bytes.
   * @return The lengths of the dynamic fields (packed into a single bytes32 value).
   * @return The dynamic (variable length) data, encoded into a sequence of bytes.
   */
  function encode(
    int32 x,
    int32 y,
    int32 z,
    string memory label
  ) internal pure returns (bytes memory, EncodedLengths, bytes memory) {
    bytes memory _staticData = encodeStatic(x, y, z);

    EncodedLengths _encodedLengths = encodeLengths(label);
    bytes memory _dynamicData = encodeDynamic(label);

    return (_staticData, _encodedLengths, _dynamicData);
  }

  /**
   * @notice Encode keys as a bytes32 array using this table's field layout.
   */
  function encodeKeyTuple(bytes32 noteId, uint16 groupId, uint16 index) internal pure returns (bytes32[] memory) {
    bytes32[] memory _keyTuple = new bytes32[](3);
    _keyTuple[0] = noteId;
    _keyTuple[1] = bytes32(uint256(groupId));
    _keyTuple[2] = bytes32(uint256(index));

    return _keyTuple;
  }
}
