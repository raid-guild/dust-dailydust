// SPDX-License-Identifier: MIT
pragma solidity >=0.8.24;

/* Autogenerated file. Do not edit manually. */

// Import store internals
import { IStore } from "@latticexyz/store/src/IStore.sol";
import { StoreSwitch } from "@latticexyz/store/src/StoreSwitch.sol";
import { StoreCore } from "@latticexyz/store/src/StoreCore.sol";
import { Bytes } from "@latticexyz/store/src/Bytes.sol";
import { Memory } from "@latticexyz/store/src/Memory.sol";
import { SliceLib } from "@latticexyz/store/src/Slice.sol";
import { EncodeArray } from "@latticexyz/store/src/tightcoder/EncodeArray.sol";
import { FieldLayout } from "@latticexyz/store/src/FieldLayout.sol";
import { Schema } from "@latticexyz/store/src/Schema.sol";
import { EncodedLengths, EncodedLengthsLib } from "@latticexyz/store/src/EncodedLengths.sol";
import { ResourceId } from "@latticexyz/store/src/ResourceId.sol";

struct WaypointGroupData {
  uint24 color;
  bool isPublic;
  string description;
  string name;
}

library WaypointGroup {
  // Hex below is the result of `WorldResourceIdLib.encode({ namespace: "rg_dd_0002", name: "WaypointGroup", typeId: RESOURCE_TABLE });`
  ResourceId constant _tableId = ResourceId.wrap(0x746272675f64645f3030303200000000576179706f696e7447726f7570000000);

  FieldLayout constant _fieldLayout =
    FieldLayout.wrap(0x0004020203010000000000000000000000000000000000000000000000000000);

  // Hex-encoded key schema of (bytes32, uint16)
  Schema constant _keySchema = Schema.wrap(0x002202005f010000000000000000000000000000000000000000000000000000);
  // Hex-encoded value schema of (uint24, bool, string, string)
  Schema constant _valueSchema = Schema.wrap(0x000402020260c5c5000000000000000000000000000000000000000000000000);

  /**
   * @notice Get the table's key field names.
   * @return keyNames An array of strings with the names of key fields.
   */
  function getKeyNames() internal pure returns (string[] memory keyNames) {
    keyNames = new string[](2);
    keyNames[0] = "noteId";
    keyNames[1] = "groupId";
  }

  /**
   * @notice Get the table's value field names.
   * @return fieldNames An array of strings with the names of value fields.
   */
  function getFieldNames() internal pure returns (string[] memory fieldNames) {
    fieldNames = new string[](4);
    fieldNames[0] = "color";
    fieldNames[1] = "isPublic";
    fieldNames[2] = "description";
    fieldNames[3] = "name";
  }

  /**
   * @notice Register the table with its config.
   */
  function register() internal {
    StoreSwitch.registerTable(_tableId, _fieldLayout, _keySchema, _valueSchema, getKeyNames(), getFieldNames());
  }

  /**
   * @notice Register the table with its config.
   */
  function _register() internal {
    StoreCore.registerTable(_tableId, _fieldLayout, _keySchema, _valueSchema, getKeyNames(), getFieldNames());
  }

  /**
   * @notice Get color.
   */
  function getColor(bytes32 noteId, uint16 groupId) internal view returns (uint24 color) {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = noteId;
    _keyTuple[1] = bytes32(uint256(groupId));

    bytes32 _blob = StoreSwitch.getStaticField(_tableId, _keyTuple, 0, _fieldLayout);
    return (uint24(bytes3(_blob)));
  }

  /**
   * @notice Get color.
   */
  function _getColor(bytes32 noteId, uint16 groupId) internal view returns (uint24 color) {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = noteId;
    _keyTuple[1] = bytes32(uint256(groupId));

    bytes32 _blob = StoreCore.getStaticField(_tableId, _keyTuple, 0, _fieldLayout);
    return (uint24(bytes3(_blob)));
  }

  /**
   * @notice Set color.
   */
  function setColor(bytes32 noteId, uint16 groupId, uint24 color) internal {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = noteId;
    _keyTuple[1] = bytes32(uint256(groupId));

    StoreSwitch.setStaticField(_tableId, _keyTuple, 0, abi.encodePacked((color)), _fieldLayout);
  }

  /**
   * @notice Set color.
   */
  function _setColor(bytes32 noteId, uint16 groupId, uint24 color) internal {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = noteId;
    _keyTuple[1] = bytes32(uint256(groupId));

    StoreCore.setStaticField(_tableId, _keyTuple, 0, abi.encodePacked((color)), _fieldLayout);
  }

  /**
   * @notice Get isPublic.
   */
  function getIsPublic(bytes32 noteId, uint16 groupId) internal view returns (bool isPublic) {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = noteId;
    _keyTuple[1] = bytes32(uint256(groupId));

    bytes32 _blob = StoreSwitch.getStaticField(_tableId, _keyTuple, 1, _fieldLayout);
    return (_toBool(uint8(bytes1(_blob))));
  }

  /**
   * @notice Get isPublic.
   */
  function _getIsPublic(bytes32 noteId, uint16 groupId) internal view returns (bool isPublic) {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = noteId;
    _keyTuple[1] = bytes32(uint256(groupId));

    bytes32 _blob = StoreCore.getStaticField(_tableId, _keyTuple, 1, _fieldLayout);
    return (_toBool(uint8(bytes1(_blob))));
  }

  /**
   * @notice Set isPublic.
   */
  function setIsPublic(bytes32 noteId, uint16 groupId, bool isPublic) internal {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = noteId;
    _keyTuple[1] = bytes32(uint256(groupId));

    StoreSwitch.setStaticField(_tableId, _keyTuple, 1, abi.encodePacked((isPublic)), _fieldLayout);
  }

  /**
   * @notice Set isPublic.
   */
  function _setIsPublic(bytes32 noteId, uint16 groupId, bool isPublic) internal {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = noteId;
    _keyTuple[1] = bytes32(uint256(groupId));

    StoreCore.setStaticField(_tableId, _keyTuple, 1, abi.encodePacked((isPublic)), _fieldLayout);
  }

  /**
   * @notice Get description.
   */
  function getDescription(bytes32 noteId, uint16 groupId) internal view returns (string memory description) {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = noteId;
    _keyTuple[1] = bytes32(uint256(groupId));

    bytes memory _blob = StoreSwitch.getDynamicField(_tableId, _keyTuple, 0);
    return (string(_blob));
  }

  /**
   * @notice Get description.
   */
  function _getDescription(bytes32 noteId, uint16 groupId) internal view returns (string memory description) {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = noteId;
    _keyTuple[1] = bytes32(uint256(groupId));

    bytes memory _blob = StoreCore.getDynamicField(_tableId, _keyTuple, 0);
    return (string(_blob));
  }

  /**
   * @notice Set description.
   */
  function setDescription(bytes32 noteId, uint16 groupId, string memory description) internal {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = noteId;
    _keyTuple[1] = bytes32(uint256(groupId));

    StoreSwitch.setDynamicField(_tableId, _keyTuple, 0, bytes((description)));
  }

  /**
   * @notice Set description.
   */
  function _setDescription(bytes32 noteId, uint16 groupId, string memory description) internal {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = noteId;
    _keyTuple[1] = bytes32(uint256(groupId));

    StoreCore.setDynamicField(_tableId, _keyTuple, 0, bytes((description)));
  }

  /**
   * @notice Get the length of description.
   */
  function lengthDescription(bytes32 noteId, uint16 groupId) internal view returns (uint256) {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = noteId;
    _keyTuple[1] = bytes32(uint256(groupId));

    uint256 _byteLength = StoreSwitch.getDynamicFieldLength(_tableId, _keyTuple, 0);
    unchecked {
      return _byteLength / 1;
    }
  }

  /**
   * @notice Get the length of description.
   */
  function _lengthDescription(bytes32 noteId, uint16 groupId) internal view returns (uint256) {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = noteId;
    _keyTuple[1] = bytes32(uint256(groupId));

    uint256 _byteLength = StoreCore.getDynamicFieldLength(_tableId, _keyTuple, 0);
    unchecked {
      return _byteLength / 1;
    }
  }

  /**
   * @notice Get an item of description.
   * @dev Reverts with Store_IndexOutOfBounds if `_index` is out of bounds for the array.
   */
  function getItemDescription(bytes32 noteId, uint16 groupId, uint256 _index) internal view returns (string memory) {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = noteId;
    _keyTuple[1] = bytes32(uint256(groupId));

    unchecked {
      bytes memory _blob = StoreSwitch.getDynamicFieldSlice(_tableId, _keyTuple, 0, _index * 1, (_index + 1) * 1);
      return (string(_blob));
    }
  }

  /**
   * @notice Get an item of description.
   * @dev Reverts with Store_IndexOutOfBounds if `_index` is out of bounds for the array.
   */
  function _getItemDescription(bytes32 noteId, uint16 groupId, uint256 _index) internal view returns (string memory) {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = noteId;
    _keyTuple[1] = bytes32(uint256(groupId));

    unchecked {
      bytes memory _blob = StoreCore.getDynamicFieldSlice(_tableId, _keyTuple, 0, _index * 1, (_index + 1) * 1);
      return (string(_blob));
    }
  }

  /**
   * @notice Push a slice to description.
   */
  function pushDescription(bytes32 noteId, uint16 groupId, string memory _slice) internal {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = noteId;
    _keyTuple[1] = bytes32(uint256(groupId));

    StoreSwitch.pushToDynamicField(_tableId, _keyTuple, 0, bytes((_slice)));
  }

  /**
   * @notice Push a slice to description.
   */
  function _pushDescription(bytes32 noteId, uint16 groupId, string memory _slice) internal {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = noteId;
    _keyTuple[1] = bytes32(uint256(groupId));

    StoreCore.pushToDynamicField(_tableId, _keyTuple, 0, bytes((_slice)));
  }

  /**
   * @notice Pop a slice from description.
   */
  function popDescription(bytes32 noteId, uint16 groupId) internal {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = noteId;
    _keyTuple[1] = bytes32(uint256(groupId));

    StoreSwitch.popFromDynamicField(_tableId, _keyTuple, 0, 1);
  }

  /**
   * @notice Pop a slice from description.
   */
  function _popDescription(bytes32 noteId, uint16 groupId) internal {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = noteId;
    _keyTuple[1] = bytes32(uint256(groupId));

    StoreCore.popFromDynamicField(_tableId, _keyTuple, 0, 1);
  }

  /**
   * @notice Update a slice of description at `_index`.
   */
  function updateDescription(bytes32 noteId, uint16 groupId, uint256 _index, string memory _slice) internal {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = noteId;
    _keyTuple[1] = bytes32(uint256(groupId));

    unchecked {
      bytes memory _encoded = bytes((_slice));
      StoreSwitch.spliceDynamicData(_tableId, _keyTuple, 0, uint40(_index * 1), uint40(_encoded.length), _encoded);
    }
  }

  /**
   * @notice Update a slice of description at `_index`.
   */
  function _updateDescription(bytes32 noteId, uint16 groupId, uint256 _index, string memory _slice) internal {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = noteId;
    _keyTuple[1] = bytes32(uint256(groupId));

    unchecked {
      bytes memory _encoded = bytes((_slice));
      StoreCore.spliceDynamicData(_tableId, _keyTuple, 0, uint40(_index * 1), uint40(_encoded.length), _encoded);
    }
  }

  /**
   * @notice Get name.
   */
  function getName(bytes32 noteId, uint16 groupId) internal view returns (string memory name) {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = noteId;
    _keyTuple[1] = bytes32(uint256(groupId));

    bytes memory _blob = StoreSwitch.getDynamicField(_tableId, _keyTuple, 1);
    return (string(_blob));
  }

  /**
   * @notice Get name.
   */
  function _getName(bytes32 noteId, uint16 groupId) internal view returns (string memory name) {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = noteId;
    _keyTuple[1] = bytes32(uint256(groupId));

    bytes memory _blob = StoreCore.getDynamicField(_tableId, _keyTuple, 1);
    return (string(_blob));
  }

  /**
   * @notice Set name.
   */
  function setName(bytes32 noteId, uint16 groupId, string memory name) internal {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = noteId;
    _keyTuple[1] = bytes32(uint256(groupId));

    StoreSwitch.setDynamicField(_tableId, _keyTuple, 1, bytes((name)));
  }

  /**
   * @notice Set name.
   */
  function _setName(bytes32 noteId, uint16 groupId, string memory name) internal {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = noteId;
    _keyTuple[1] = bytes32(uint256(groupId));

    StoreCore.setDynamicField(_tableId, _keyTuple, 1, bytes((name)));
  }

  /**
   * @notice Get the length of name.
   */
  function lengthName(bytes32 noteId, uint16 groupId) internal view returns (uint256) {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = noteId;
    _keyTuple[1] = bytes32(uint256(groupId));

    uint256 _byteLength = StoreSwitch.getDynamicFieldLength(_tableId, _keyTuple, 1);
    unchecked {
      return _byteLength / 1;
    }
  }

  /**
   * @notice Get the length of name.
   */
  function _lengthName(bytes32 noteId, uint16 groupId) internal view returns (uint256) {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = noteId;
    _keyTuple[1] = bytes32(uint256(groupId));

    uint256 _byteLength = StoreCore.getDynamicFieldLength(_tableId, _keyTuple, 1);
    unchecked {
      return _byteLength / 1;
    }
  }

  /**
   * @notice Get an item of name.
   * @dev Reverts with Store_IndexOutOfBounds if `_index` is out of bounds for the array.
   */
  function getItemName(bytes32 noteId, uint16 groupId, uint256 _index) internal view returns (string memory) {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = noteId;
    _keyTuple[1] = bytes32(uint256(groupId));

    unchecked {
      bytes memory _blob = StoreSwitch.getDynamicFieldSlice(_tableId, _keyTuple, 1, _index * 1, (_index + 1) * 1);
      return (string(_blob));
    }
  }

  /**
   * @notice Get an item of name.
   * @dev Reverts with Store_IndexOutOfBounds if `_index` is out of bounds for the array.
   */
  function _getItemName(bytes32 noteId, uint16 groupId, uint256 _index) internal view returns (string memory) {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = noteId;
    _keyTuple[1] = bytes32(uint256(groupId));

    unchecked {
      bytes memory _blob = StoreCore.getDynamicFieldSlice(_tableId, _keyTuple, 1, _index * 1, (_index + 1) * 1);
      return (string(_blob));
    }
  }

  /**
   * @notice Push a slice to name.
   */
  function pushName(bytes32 noteId, uint16 groupId, string memory _slice) internal {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = noteId;
    _keyTuple[1] = bytes32(uint256(groupId));

    StoreSwitch.pushToDynamicField(_tableId, _keyTuple, 1, bytes((_slice)));
  }

  /**
   * @notice Push a slice to name.
   */
  function _pushName(bytes32 noteId, uint16 groupId, string memory _slice) internal {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = noteId;
    _keyTuple[1] = bytes32(uint256(groupId));

    StoreCore.pushToDynamicField(_tableId, _keyTuple, 1, bytes((_slice)));
  }

  /**
   * @notice Pop a slice from name.
   */
  function popName(bytes32 noteId, uint16 groupId) internal {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = noteId;
    _keyTuple[1] = bytes32(uint256(groupId));

    StoreSwitch.popFromDynamicField(_tableId, _keyTuple, 1, 1);
  }

  /**
   * @notice Pop a slice from name.
   */
  function _popName(bytes32 noteId, uint16 groupId) internal {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = noteId;
    _keyTuple[1] = bytes32(uint256(groupId));

    StoreCore.popFromDynamicField(_tableId, _keyTuple, 1, 1);
  }

  /**
   * @notice Update a slice of name at `_index`.
   */
  function updateName(bytes32 noteId, uint16 groupId, uint256 _index, string memory _slice) internal {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = noteId;
    _keyTuple[1] = bytes32(uint256(groupId));

    unchecked {
      bytes memory _encoded = bytes((_slice));
      StoreSwitch.spliceDynamicData(_tableId, _keyTuple, 1, uint40(_index * 1), uint40(_encoded.length), _encoded);
    }
  }

  /**
   * @notice Update a slice of name at `_index`.
   */
  function _updateName(bytes32 noteId, uint16 groupId, uint256 _index, string memory _slice) internal {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = noteId;
    _keyTuple[1] = bytes32(uint256(groupId));

    unchecked {
      bytes memory _encoded = bytes((_slice));
      StoreCore.spliceDynamicData(_tableId, _keyTuple, 1, uint40(_index * 1), uint40(_encoded.length), _encoded);
    }
  }

  /**
   * @notice Get the full data.
   */
  function get(bytes32 noteId, uint16 groupId) internal view returns (WaypointGroupData memory _table) {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = noteId;
    _keyTuple[1] = bytes32(uint256(groupId));

    (bytes memory _staticData, EncodedLengths _encodedLengths, bytes memory _dynamicData) = StoreSwitch.getRecord(
      _tableId,
      _keyTuple,
      _fieldLayout
    );
    return decode(_staticData, _encodedLengths, _dynamicData);
  }

  /**
   * @notice Get the full data.
   */
  function _get(bytes32 noteId, uint16 groupId) internal view returns (WaypointGroupData memory _table) {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = noteId;
    _keyTuple[1] = bytes32(uint256(groupId));

    (bytes memory _staticData, EncodedLengths _encodedLengths, bytes memory _dynamicData) = StoreCore.getRecord(
      _tableId,
      _keyTuple,
      _fieldLayout
    );
    return decode(_staticData, _encodedLengths, _dynamicData);
  }

  /**
   * @notice Set the full data using individual values.
   */
  function set(
    bytes32 noteId,
    uint16 groupId,
    uint24 color,
    bool isPublic,
    string memory description,
    string memory name
  ) internal {
    bytes memory _staticData = encodeStatic(color, isPublic);

    EncodedLengths _encodedLengths = encodeLengths(description, name);
    bytes memory _dynamicData = encodeDynamic(description, name);

    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = noteId;
    _keyTuple[1] = bytes32(uint256(groupId));

    StoreSwitch.setRecord(_tableId, _keyTuple, _staticData, _encodedLengths, _dynamicData);
  }

  /**
   * @notice Set the full data using individual values.
   */
  function _set(
    bytes32 noteId,
    uint16 groupId,
    uint24 color,
    bool isPublic,
    string memory description,
    string memory name
  ) internal {
    bytes memory _staticData = encodeStatic(color, isPublic);

    EncodedLengths _encodedLengths = encodeLengths(description, name);
    bytes memory _dynamicData = encodeDynamic(description, name);

    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = noteId;
    _keyTuple[1] = bytes32(uint256(groupId));

    StoreCore.setRecord(_tableId, _keyTuple, _staticData, _encodedLengths, _dynamicData, _fieldLayout);
  }

  /**
   * @notice Set the full data using the data struct.
   */
  function set(bytes32 noteId, uint16 groupId, WaypointGroupData memory _table) internal {
    bytes memory _staticData = encodeStatic(_table.color, _table.isPublic);

    EncodedLengths _encodedLengths = encodeLengths(_table.description, _table.name);
    bytes memory _dynamicData = encodeDynamic(_table.description, _table.name);

    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = noteId;
    _keyTuple[1] = bytes32(uint256(groupId));

    StoreSwitch.setRecord(_tableId, _keyTuple, _staticData, _encodedLengths, _dynamicData);
  }

  /**
   * @notice Set the full data using the data struct.
   */
  function _set(bytes32 noteId, uint16 groupId, WaypointGroupData memory _table) internal {
    bytes memory _staticData = encodeStatic(_table.color, _table.isPublic);

    EncodedLengths _encodedLengths = encodeLengths(_table.description, _table.name);
    bytes memory _dynamicData = encodeDynamic(_table.description, _table.name);

    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = noteId;
    _keyTuple[1] = bytes32(uint256(groupId));

    StoreCore.setRecord(_tableId, _keyTuple, _staticData, _encodedLengths, _dynamicData, _fieldLayout);
  }

  /**
   * @notice Decode the tightly packed blob of static data using this table's field layout.
   */
  function decodeStatic(bytes memory _blob) internal pure returns (uint24 color, bool isPublic) {
    color = (uint24(Bytes.getBytes3(_blob, 0)));

    isPublic = (_toBool(uint8(Bytes.getBytes1(_blob, 3))));
  }

  /**
   * @notice Decode the tightly packed blob of dynamic data using the encoded lengths.
   */
  function decodeDynamic(
    EncodedLengths _encodedLengths,
    bytes memory _blob
  ) internal pure returns (string memory description, string memory name) {
    uint256 _start;
    uint256 _end;
    unchecked {
      _end = _encodedLengths.atIndex(0);
    }
    description = (string(SliceLib.getSubslice(_blob, _start, _end).toBytes()));

    _start = _end;
    unchecked {
      _end += _encodedLengths.atIndex(1);
    }
    name = (string(SliceLib.getSubslice(_blob, _start, _end).toBytes()));
  }

  /**
   * @notice Decode the tightly packed blobs using this table's field layout.
   * @param _staticData Tightly packed static fields.
   * @param _encodedLengths Encoded lengths of dynamic fields.
   * @param _dynamicData Tightly packed dynamic fields.
   */
  function decode(
    bytes memory _staticData,
    EncodedLengths _encodedLengths,
    bytes memory _dynamicData
  ) internal pure returns (WaypointGroupData memory _table) {
    (_table.color, _table.isPublic) = decodeStatic(_staticData);

    (_table.description, _table.name) = decodeDynamic(_encodedLengths, _dynamicData);
  }

  /**
   * @notice Delete all data for given keys.
   */
  function deleteRecord(bytes32 noteId, uint16 groupId) internal {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = noteId;
    _keyTuple[1] = bytes32(uint256(groupId));

    StoreSwitch.deleteRecord(_tableId, _keyTuple);
  }

  /**
   * @notice Delete all data for given keys.
   */
  function _deleteRecord(bytes32 noteId, uint16 groupId) internal {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = noteId;
    _keyTuple[1] = bytes32(uint256(groupId));

    StoreCore.deleteRecord(_tableId, _keyTuple, _fieldLayout);
  }

  /**
   * @notice Tightly pack static (fixed length) data using this table's schema.
   * @return The static data, encoded into a sequence of bytes.
   */
  function encodeStatic(uint24 color, bool isPublic) internal pure returns (bytes memory) {
    return abi.encodePacked(color, isPublic);
  }

  /**
   * @notice Tightly pack dynamic data lengths using this table's schema.
   * @return _encodedLengths The lengths of the dynamic fields (packed into a single bytes32 value).
   */
  function encodeLengths(
    string memory description,
    string memory name
  ) internal pure returns (EncodedLengths _encodedLengths) {
    // Lengths are effectively checked during copy by 2**40 bytes exceeding gas limits
    unchecked {
      _encodedLengths = EncodedLengthsLib.pack(bytes(description).length, bytes(name).length);
    }
  }

  /**
   * @notice Tightly pack dynamic (variable length) data using this table's schema.
   * @return The dynamic data, encoded into a sequence of bytes.
   */
  function encodeDynamic(string memory description, string memory name) internal pure returns (bytes memory) {
    return abi.encodePacked(bytes((description)), bytes((name)));
  }

  /**
   * @notice Encode all of a record's fields.
   * @return The static (fixed length) data, encoded into a sequence of bytes.
   * @return The lengths of the dynamic fields (packed into a single bytes32 value).
   * @return The dynamic (variable length) data, encoded into a sequence of bytes.
   */
  function encode(
    uint24 color,
    bool isPublic,
    string memory description,
    string memory name
  ) internal pure returns (bytes memory, EncodedLengths, bytes memory) {
    bytes memory _staticData = encodeStatic(color, isPublic);

    EncodedLengths _encodedLengths = encodeLengths(description, name);
    bytes memory _dynamicData = encodeDynamic(description, name);

    return (_staticData, _encodedLengths, _dynamicData);
  }

  /**
   * @notice Encode keys as a bytes32 array using this table's field layout.
   */
  function encodeKeyTuple(bytes32 noteId, uint16 groupId) internal pure returns (bytes32[] memory) {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = noteId;
    _keyTuple[1] = bytes32(uint256(groupId));

    return _keyTuple;
  }
}

/**
 * @notice Cast a value to a bool.
 * @dev Boolean values are encoded as uint8 (1 = true, 0 = false), but Solidity doesn't allow casting between uint8 and bool.
 * @param value The uint8 value to convert.
 * @return result The boolean value.
 */
function _toBool(uint8 value) pure returns (bool result) {
  assembly {
    result := value
  }
}
