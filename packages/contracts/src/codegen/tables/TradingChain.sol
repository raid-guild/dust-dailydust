// SPDX-License-Identifier: MIT
pragma solidity >=0.8.24;

/* Autogenerated file. Do not edit manually. */

// Import store internals
import { IStore } from "@latticexyz/store/src/IStore.sol";
import { StoreSwitch } from "@latticexyz/store/src/StoreSwitch.sol";
import { StoreCore } from "@latticexyz/store/src/StoreCore.sol";
import { Bytes } from "@latticexyz/store/src/Bytes.sol";
import { Memory } from "@latticexyz/store/src/Memory.sol";
import { SliceLib } from "@latticexyz/store/src/Slice.sol";
import { EncodeArray } from "@latticexyz/store/src/tightcoder/EncodeArray.sol";
import { FieldLayout } from "@latticexyz/store/src/FieldLayout.sol";
import { Schema } from "@latticexyz/store/src/Schema.sol";
import { EncodedLengths, EncodedLengthsLib } from "@latticexyz/store/src/EncodedLengths.sol";
import { ResourceId } from "@latticexyz/store/src/ResourceId.sol";

// Import user types
import { EntityId } from "@dust/world/src/types/EntityId.sol";
import { ObjectType } from "@dust/world/src/types/ObjectType.sol";

library TradingChain {
  // Hex below is the result of `WorldResourceIdLib.encode({ namespace: "my_namespace", name: "TradingChain", typeId: RESOURCE_TABLE });`
  ResourceId constant _tableId = ResourceId.wrap(0x74626d795f6e616d657370616365000054726164696e67436861696e00000000);

  FieldLayout constant _fieldLayout =
    FieldLayout.wrap(0x0002010002000000000000000000000000000000000000000000000000000000);

  // Hex-encoded key schema of (bytes32, uint16)
  Schema constant _keySchema = Schema.wrap(0x002202005f010000000000000000000000000000000000000000000000000000);
  // Hex-encoded value schema of (uint16)
  Schema constant _valueSchema = Schema.wrap(0x0002010001000000000000000000000000000000000000000000000000000000);

  /**
   * @notice Get the table's key field names.
   * @return keyNames An array of strings with the names of key fields.
   */
  function getKeyNames() internal pure returns (string[] memory keyNames) {
    keyNames = new string[](2);
    keyNames[0] = "chest";
    keyNames[1] = "fromItem";
  }

  /**
   * @notice Get the table's value field names.
   * @return fieldNames An array of strings with the names of value fields.
   */
  function getFieldNames() internal pure returns (string[] memory fieldNames) {
    fieldNames = new string[](1);
    fieldNames[0] = "toItem";
  }

  /**
   * @notice Register the table with its config.
   */
  function register() internal {
    StoreSwitch.registerTable(_tableId, _fieldLayout, _keySchema, _valueSchema, getKeyNames(), getFieldNames());
  }

  /**
   * @notice Register the table with its config.
   */
  function _register() internal {
    StoreCore.registerTable(_tableId, _fieldLayout, _keySchema, _valueSchema, getKeyNames(), getFieldNames());
  }

  /**
   * @notice Get toItem.
   */
  function getToItem(EntityId chest, ObjectType fromItem) internal view returns (ObjectType toItem) {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = EntityId.unwrap(chest);
    _keyTuple[1] = bytes32(uint256(ObjectType.unwrap(fromItem)));

    bytes32 _blob = StoreSwitch.getStaticField(_tableId, _keyTuple, 0, _fieldLayout);
    return ObjectType.wrap(uint16(bytes2(_blob)));
  }

  /**
   * @notice Get toItem.
   */
  function _getToItem(EntityId chest, ObjectType fromItem) internal view returns (ObjectType toItem) {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = EntityId.unwrap(chest);
    _keyTuple[1] = bytes32(uint256(ObjectType.unwrap(fromItem)));

    bytes32 _blob = StoreCore.getStaticField(_tableId, _keyTuple, 0, _fieldLayout);
    return ObjectType.wrap(uint16(bytes2(_blob)));
  }

  /**
   * @notice Get toItem.
   */
  function get(EntityId chest, ObjectType fromItem) internal view returns (ObjectType toItem) {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = EntityId.unwrap(chest);
    _keyTuple[1] = bytes32(uint256(ObjectType.unwrap(fromItem)));

    bytes32 _blob = StoreSwitch.getStaticField(_tableId, _keyTuple, 0, _fieldLayout);
    return ObjectType.wrap(uint16(bytes2(_blob)));
  }

  /**
   * @notice Get toItem.
   */
  function _get(EntityId chest, ObjectType fromItem) internal view returns (ObjectType toItem) {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = EntityId.unwrap(chest);
    _keyTuple[1] = bytes32(uint256(ObjectType.unwrap(fromItem)));

    bytes32 _blob = StoreCore.getStaticField(_tableId, _keyTuple, 0, _fieldLayout);
    return ObjectType.wrap(uint16(bytes2(_blob)));
  }

  /**
   * @notice Set toItem.
   */
  function setToItem(EntityId chest, ObjectType fromItem, ObjectType toItem) internal {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = EntityId.unwrap(chest);
    _keyTuple[1] = bytes32(uint256(ObjectType.unwrap(fromItem)));

    StoreSwitch.setStaticField(_tableId, _keyTuple, 0, abi.encodePacked(ObjectType.unwrap(toItem)), _fieldLayout);
  }

  /**
   * @notice Set toItem.
   */
  function _setToItem(EntityId chest, ObjectType fromItem, ObjectType toItem) internal {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = EntityId.unwrap(chest);
    _keyTuple[1] = bytes32(uint256(ObjectType.unwrap(fromItem)));

    StoreCore.setStaticField(_tableId, _keyTuple, 0, abi.encodePacked(ObjectType.unwrap(toItem)), _fieldLayout);
  }

  /**
   * @notice Set toItem.
   */
  function set(EntityId chest, ObjectType fromItem, ObjectType toItem) internal {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = EntityId.unwrap(chest);
    _keyTuple[1] = bytes32(uint256(ObjectType.unwrap(fromItem)));

    StoreSwitch.setStaticField(_tableId, _keyTuple, 0, abi.encodePacked(ObjectType.unwrap(toItem)), _fieldLayout);
  }

  /**
   * @notice Set toItem.
   */
  function _set(EntityId chest, ObjectType fromItem, ObjectType toItem) internal {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = EntityId.unwrap(chest);
    _keyTuple[1] = bytes32(uint256(ObjectType.unwrap(fromItem)));

    StoreCore.setStaticField(_tableId, _keyTuple, 0, abi.encodePacked(ObjectType.unwrap(toItem)), _fieldLayout);
  }

  /**
   * @notice Delete all data for given keys.
   */
  function deleteRecord(EntityId chest, ObjectType fromItem) internal {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = EntityId.unwrap(chest);
    _keyTuple[1] = bytes32(uint256(ObjectType.unwrap(fromItem)));

    StoreSwitch.deleteRecord(_tableId, _keyTuple);
  }

  /**
   * @notice Delete all data for given keys.
   */
  function _deleteRecord(EntityId chest, ObjectType fromItem) internal {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = EntityId.unwrap(chest);
    _keyTuple[1] = bytes32(uint256(ObjectType.unwrap(fromItem)));

    StoreCore.deleteRecord(_tableId, _keyTuple, _fieldLayout);
  }

  /**
   * @notice Tightly pack static (fixed length) data using this table's schema.
   * @return The static data, encoded into a sequence of bytes.
   */
  function encodeStatic(ObjectType toItem) internal pure returns (bytes memory) {
    return abi.encodePacked(toItem);
  }

  /**
   * @notice Encode all of a record's fields.
   * @return The static (fixed length) data, encoded into a sequence of bytes.
   * @return The lengths of the dynamic fields (packed into a single bytes32 value).
   * @return The dynamic (variable length) data, encoded into a sequence of bytes.
   */
  function encode(ObjectType toItem) internal pure returns (bytes memory, EncodedLengths, bytes memory) {
    bytes memory _staticData = encodeStatic(toItem);

    EncodedLengths _encodedLengths;
    bytes memory _dynamicData;

    return (_staticData, _encodedLengths, _dynamicData);
  }

  /**
   * @notice Encode keys as a bytes32 array using this table's field layout.
   */
  function encodeKeyTuple(EntityId chest, ObjectType fromItem) internal pure returns (bytes32[] memory) {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = EntityId.unwrap(chest);
    _keyTuple[1] = bytes32(uint256(ObjectType.unwrap(fromItem)));

    return _keyTuple;
  }
}
