// SPDX-License-Identifier: MIT
pragma solidity >=0.8.24;

/* Autogenerated file. Do not edit manually. */

import { EditorSystem } from "../../systems/EditorSystem.sol";
import { EditorRoleData } from "../tables/EditorRole.sol";
import { revertWithBytes } from "@latticexyz/world/src/revertWithBytes.sol";
import { IWorldCall } from "@latticexyz/world/src/IWorldKernel.sol";
import { SystemCall } from "@latticexyz/world/src/SystemCall.sol";
import { WorldContextConsumerLib } from "@latticexyz/world/src/WorldContext.sol";
import { Systems } from "@latticexyz/world/src/codegen/tables/Systems.sol";
import { ResourceId } from "@latticexyz/store/src/ResourceId.sol";
import { StoreSwitch } from "@latticexyz/store/src/StoreSwitch.sol";

type EditorSystemType is bytes32;

// equivalent to WorldResourceIdLib.encode({ typeId: RESOURCE_SYSTEM, namespace: "dailydust", name: "EditorSystem" }))
EditorSystemType constant editorSystem = EditorSystemType.wrap(
  0x73796461696c79647573740000000000456469746f7253797374656d00000000
);

struct CallWrapper {
  ResourceId systemId;
  address from;
}

struct RootCallWrapper {
  ResourceId systemId;
  address from;
}

/**
 * @title EditorSystemLib
 * @author MUD (https://mud.dev) by Lattice (https://lattice.xyz)
 * @dev This library is automatically generated from the corresponding system contract. Do not edit manually.
 */
library EditorSystemLib {
  error EditorSystemLib_CallingFromRootSystem();

  function grantRole(EditorSystemType self, address user, uint8 role) internal {
    return CallWrapper(self.toResourceId(), address(0)).grantRole(user, role);
  }

  function revokeRole(EditorSystemType self, address user) internal {
    return CallWrapper(self.toResourceId(), address(0)).revokeRole(user);
  }

  function initializeOwner(EditorSystemType self, address initialOwner) internal {
    return CallWrapper(self.toResourceId(), address(0)).initializeOwner(initialOwner);
  }

  function getUserRole(EditorSystemType self, address user) internal view returns (EditorRoleData memory __auxRet0) {
    return CallWrapper(self.toResourceId(), address(0)).getUserRole(user);
  }

  function hasRole(EditorSystemType self, address user, uint8 requiredRole) internal view returns (bool __auxRet0) {
    return CallWrapper(self.toResourceId(), address(0)).hasRole(user, requiredRole);
  }

  function isEditor(EditorSystemType self, address user) internal view returns (bool __auxRet0) {
    return CallWrapper(self.toResourceId(), address(0)).isEditor(user);
  }

  function isAdmin(EditorSystemType self, address user) internal view returns (bool __auxRet0) {
    return CallWrapper(self.toResourceId(), address(0)).isAdmin(user);
  }

  function isOwner(EditorSystemType self, address user) internal view returns (bool __auxRet0) {
    return CallWrapper(self.toResourceId(), address(0)).isOwner(user);
  }

  function transferOwnership(EditorSystemType self, address newOwner) internal {
    return CallWrapper(self.toResourceId(), address(0)).transferOwnership(newOwner);
  }

  function grantRole(CallWrapper memory self, address user, uint8 role) internal {
    // if the contract calling this function is a root system, it should use `callAsRoot`
    if (address(_world()) == address(this)) revert EditorSystemLib_CallingFromRootSystem();

    bytes memory systemCall = abi.encodeCall(_grantRole_address_uint8.grantRole, (user, role));
    self.from == address(0)
      ? _world().call(self.systemId, systemCall)
      : _world().callFrom(self.from, self.systemId, systemCall);
  }

  function revokeRole(CallWrapper memory self, address user) internal {
    // if the contract calling this function is a root system, it should use `callAsRoot`
    if (address(_world()) == address(this)) revert EditorSystemLib_CallingFromRootSystem();

    bytes memory systemCall = abi.encodeCall(_revokeRole_address.revokeRole, (user));
    self.from == address(0)
      ? _world().call(self.systemId, systemCall)
      : _world().callFrom(self.from, self.systemId, systemCall);
  }

  function initializeOwner(CallWrapper memory self, address initialOwner) internal {
    // if the contract calling this function is a root system, it should use `callAsRoot`
    if (address(_world()) == address(this)) revert EditorSystemLib_CallingFromRootSystem();

    bytes memory systemCall = abi.encodeCall(_initializeOwner_address.initializeOwner, (initialOwner));
    self.from == address(0)
      ? _world().call(self.systemId, systemCall)
      : _world().callFrom(self.from, self.systemId, systemCall);
  }

  function getUserRole(CallWrapper memory self, address user) internal view returns (EditorRoleData memory __auxRet0) {
    // if the contract calling this function is a root system, it should use `callAsRoot`
    if (address(_world()) == address(this)) revert EditorSystemLib_CallingFromRootSystem();

    bytes memory systemCall = abi.encodeCall(_getUserRole_address.getUserRole, (user));
    bytes memory worldCall = self.from == address(0)
      ? abi.encodeCall(IWorldCall.call, (self.systemId, systemCall))
      : abi.encodeCall(IWorldCall.callFrom, (self.from, self.systemId, systemCall));
    (bool success, bytes memory returnData) = address(_world()).staticcall(worldCall);
    if (!success) revertWithBytes(returnData);

    bytes memory result = abi.decode(returnData, (bytes));
    // skip decoding an empty result, which can happen after expectRevert
    if (result.length != 0) {
      return abi.decode(result, (EditorRoleData));
    }
  }

  function hasRole(CallWrapper memory self, address user, uint8 requiredRole) internal view returns (bool __auxRet0) {
    // if the contract calling this function is a root system, it should use `callAsRoot`
    if (address(_world()) == address(this)) revert EditorSystemLib_CallingFromRootSystem();

    bytes memory systemCall = abi.encodeCall(_hasRole_address_uint8.hasRole, (user, requiredRole));
    bytes memory worldCall = self.from == address(0)
      ? abi.encodeCall(IWorldCall.call, (self.systemId, systemCall))
      : abi.encodeCall(IWorldCall.callFrom, (self.from, self.systemId, systemCall));
    (bool success, bytes memory returnData) = address(_world()).staticcall(worldCall);
    if (!success) revertWithBytes(returnData);

    bytes memory result = abi.decode(returnData, (bytes));
    // skip decoding an empty result, which can happen after expectRevert
    if (result.length != 0) {
      return abi.decode(result, (bool));
    }
  }

  function isEditor(CallWrapper memory self, address user) internal view returns (bool __auxRet0) {
    // if the contract calling this function is a root system, it should use `callAsRoot`
    if (address(_world()) == address(this)) revert EditorSystemLib_CallingFromRootSystem();

    bytes memory systemCall = abi.encodeCall(_isEditor_address.isEditor, (user));
    bytes memory worldCall = self.from == address(0)
      ? abi.encodeCall(IWorldCall.call, (self.systemId, systemCall))
      : abi.encodeCall(IWorldCall.callFrom, (self.from, self.systemId, systemCall));
    (bool success, bytes memory returnData) = address(_world()).staticcall(worldCall);
    if (!success) revertWithBytes(returnData);

    bytes memory result = abi.decode(returnData, (bytes));
    // skip decoding an empty result, which can happen after expectRevert
    if (result.length != 0) {
      return abi.decode(result, (bool));
    }
  }

  function isAdmin(CallWrapper memory self, address user) internal view returns (bool __auxRet0) {
    // if the contract calling this function is a root system, it should use `callAsRoot`
    if (address(_world()) == address(this)) revert EditorSystemLib_CallingFromRootSystem();

    bytes memory systemCall = abi.encodeCall(_isAdmin_address.isAdmin, (user));
    bytes memory worldCall = self.from == address(0)
      ? abi.encodeCall(IWorldCall.call, (self.systemId, systemCall))
      : abi.encodeCall(IWorldCall.callFrom, (self.from, self.systemId, systemCall));
    (bool success, bytes memory returnData) = address(_world()).staticcall(worldCall);
    if (!success) revertWithBytes(returnData);

    bytes memory result = abi.decode(returnData, (bytes));
    // skip decoding an empty result, which can happen after expectRevert
    if (result.length != 0) {
      return abi.decode(result, (bool));
    }
  }

  function isOwner(CallWrapper memory self, address user) internal view returns (bool __auxRet0) {
    // if the contract calling this function is a root system, it should use `callAsRoot`
    if (address(_world()) == address(this)) revert EditorSystemLib_CallingFromRootSystem();

    bytes memory systemCall = abi.encodeCall(_isOwner_address.isOwner, (user));
    bytes memory worldCall = self.from == address(0)
      ? abi.encodeCall(IWorldCall.call, (self.systemId, systemCall))
      : abi.encodeCall(IWorldCall.callFrom, (self.from, self.systemId, systemCall));
    (bool success, bytes memory returnData) = address(_world()).staticcall(worldCall);
    if (!success) revertWithBytes(returnData);

    bytes memory result = abi.decode(returnData, (bytes));
    // skip decoding an empty result, which can happen after expectRevert
    if (result.length != 0) {
      return abi.decode(result, (bool));
    }
  }

  function transferOwnership(CallWrapper memory self, address newOwner) internal {
    // if the contract calling this function is a root system, it should use `callAsRoot`
    if (address(_world()) == address(this)) revert EditorSystemLib_CallingFromRootSystem();

    bytes memory systemCall = abi.encodeCall(_transferOwnership_address.transferOwnership, (newOwner));
    self.from == address(0)
      ? _world().call(self.systemId, systemCall)
      : _world().callFrom(self.from, self.systemId, systemCall);
  }

  function grantRole(RootCallWrapper memory self, address user, uint8 role) internal {
    bytes memory systemCall = abi.encodeCall(_grantRole_address_uint8.grantRole, (user, role));
    SystemCall.callWithHooksOrRevert(self.from, self.systemId, systemCall, msg.value);
  }

  function revokeRole(RootCallWrapper memory self, address user) internal {
    bytes memory systemCall = abi.encodeCall(_revokeRole_address.revokeRole, (user));
    SystemCall.callWithHooksOrRevert(self.from, self.systemId, systemCall, msg.value);
  }

  function initializeOwner(RootCallWrapper memory self, address initialOwner) internal {
    bytes memory systemCall = abi.encodeCall(_initializeOwner_address.initializeOwner, (initialOwner));
    SystemCall.callWithHooksOrRevert(self.from, self.systemId, systemCall, msg.value);
  }

  function getUserRole(
    RootCallWrapper memory self,
    address user
  ) internal view returns (EditorRoleData memory __auxRet0) {
    bytes memory systemCall = abi.encodeCall(_getUserRole_address.getUserRole, (user));

    bytes memory result = SystemCall.staticcallOrRevert(self.from, self.systemId, systemCall);
    // skip decoding an empty result, which can happen after expectRevert
    if (result.length != 0) {
      return abi.decode(result, (EditorRoleData));
    }
  }

  function hasRole(
    RootCallWrapper memory self,
    address user,
    uint8 requiredRole
  ) internal view returns (bool __auxRet0) {
    bytes memory systemCall = abi.encodeCall(_hasRole_address_uint8.hasRole, (user, requiredRole));

    bytes memory result = SystemCall.staticcallOrRevert(self.from, self.systemId, systemCall);
    // skip decoding an empty result, which can happen after expectRevert
    if (result.length != 0) {
      return abi.decode(result, (bool));
    }
  }

  function isEditor(RootCallWrapper memory self, address user) internal view returns (bool __auxRet0) {
    bytes memory systemCall = abi.encodeCall(_isEditor_address.isEditor, (user));

    bytes memory result = SystemCall.staticcallOrRevert(self.from, self.systemId, systemCall);
    // skip decoding an empty result, which can happen after expectRevert
    if (result.length != 0) {
      return abi.decode(result, (bool));
    }
  }

  function isAdmin(RootCallWrapper memory self, address user) internal view returns (bool __auxRet0) {
    bytes memory systemCall = abi.encodeCall(_isAdmin_address.isAdmin, (user));

    bytes memory result = SystemCall.staticcallOrRevert(self.from, self.systemId, systemCall);
    // skip decoding an empty result, which can happen after expectRevert
    if (result.length != 0) {
      return abi.decode(result, (bool));
    }
  }

  function isOwner(RootCallWrapper memory self, address user) internal view returns (bool __auxRet0) {
    bytes memory systemCall = abi.encodeCall(_isOwner_address.isOwner, (user));

    bytes memory result = SystemCall.staticcallOrRevert(self.from, self.systemId, systemCall);
    // skip decoding an empty result, which can happen after expectRevert
    if (result.length != 0) {
      return abi.decode(result, (bool));
    }
  }

  function transferOwnership(RootCallWrapper memory self, address newOwner) internal {
    bytes memory systemCall = abi.encodeCall(_transferOwnership_address.transferOwnership, (newOwner));
    SystemCall.callWithHooksOrRevert(self.from, self.systemId, systemCall, msg.value);
  }

  function callFrom(EditorSystemType self, address from) internal pure returns (CallWrapper memory) {
    return CallWrapper(self.toResourceId(), from);
  }

  function callAsRoot(EditorSystemType self) internal view returns (RootCallWrapper memory) {
    return RootCallWrapper(self.toResourceId(), WorldContextConsumerLib._msgSender());
  }

  function callAsRootFrom(EditorSystemType self, address from) internal pure returns (RootCallWrapper memory) {
    return RootCallWrapper(self.toResourceId(), from);
  }

  function toResourceId(EditorSystemType self) internal pure returns (ResourceId) {
    return ResourceId.wrap(EditorSystemType.unwrap(self));
  }

  function fromResourceId(ResourceId resourceId) internal pure returns (EditorSystemType) {
    return EditorSystemType.wrap(resourceId.unwrap());
  }

  function getAddress(EditorSystemType self) internal view returns (address) {
    return Systems.getSystem(self.toResourceId());
  }

  function _world() private view returns (IWorldCall) {
    return IWorldCall(StoreSwitch.getStoreAddress());
  }
}

/**
 * System Function Interfaces
 *
 * We generate an interface for each system function, which is then used for encoding system calls.
 * This is necessary to handle function overloading correctly (which abi.encodeCall cannot).
 *
 * Each interface is uniquely named based on the function name and parameters to prevent collisions.
 */

interface _grantRole_address_uint8 {
  function grantRole(address user, uint8 role) external;
}

interface _revokeRole_address {
  function revokeRole(address user) external;
}

interface _initializeOwner_address {
  function initializeOwner(address initialOwner) external;
}

interface _getUserRole_address {
  function getUserRole(address user) external;
}

interface _hasRole_address_uint8 {
  function hasRole(address user, uint8 requiredRole) external;
}

interface _isEditor_address {
  function isEditor(address user) external;
}

interface _isAdmin_address {
  function isAdmin(address user) external;
}

interface _isOwner_address {
  function isOwner(address user) external;
}

interface _transferOwnership_address {
  function transferOwnership(address newOwner) external;
}

using EditorSystemLib for EditorSystemType global;
using EditorSystemLib for CallWrapper global;
using EditorSystemLib for RootCallWrapper global;
