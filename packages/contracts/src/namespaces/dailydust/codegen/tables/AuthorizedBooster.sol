// SPDX-License-Identifier: MIT
pragma solidity >=0.8.24;

/* Autogenerated file. Do not edit manually. */

// Import store internals
import { IStore } from "@latticexyz/store/src/IStore.sol";
import { StoreSwitch } from "@latticexyz/store/src/StoreSwitch.sol";
import { StoreCore } from "@latticexyz/store/src/StoreCore.sol";
import { Bytes } from "@latticexyz/store/src/Bytes.sol";
import { Memory } from "@latticexyz/store/src/Memory.sol";
import { SliceLib } from "@latticexyz/store/src/Slice.sol";
import { EncodeArray } from "@latticexyz/store/src/tightcoder/EncodeArray.sol";
import { FieldLayout } from "@latticexyz/store/src/FieldLayout.sol";
import { Schema } from "@latticexyz/store/src/Schema.sol";
import { EncodedLengths, EncodedLengthsLib } from "@latticexyz/store/src/EncodedLengths.sol";
import { ResourceId } from "@latticexyz/store/src/ResourceId.sol";

struct AuthorizedBoosterData {
  uint8 programType;
  uint256 maxBoostAmount;
  address grantedBy;
  uint64 grantedAt;
  uint8 isActive;
}

library AuthorizedBooster {
  // Hex below is the result of `WorldResourceIdLib.encode({ namespace: "dailydust", name: "AuthorizedBooste", typeId: RESOURCE_TABLE });`
  ResourceId constant _tableId = ResourceId.wrap(0x74626461696c79647573740000000000417574686f72697a6564426f6f737465);

  FieldLayout constant _fieldLayout =
    FieldLayout.wrap(0x003e050001201408010000000000000000000000000000000000000000000000);

  // Hex-encoded key schema of (address)
  Schema constant _keySchema = Schema.wrap(0x0014010061000000000000000000000000000000000000000000000000000000);
  // Hex-encoded value schema of (uint8, uint256, address, uint64, uint8)
  Schema constant _valueSchema = Schema.wrap(0x003e0500001f6107000000000000000000000000000000000000000000000000);

  /**
   * @notice Get the table's key field names.
   * @return keyNames An array of strings with the names of key fields.
   */
  function getKeyNames() internal pure returns (string[] memory keyNames) {
    keyNames = new string[](1);
    keyNames[0] = "programAddress";
  }

  /**
   * @notice Get the table's value field names.
   * @return fieldNames An array of strings with the names of value fields.
   */
  function getFieldNames() internal pure returns (string[] memory fieldNames) {
    fieldNames = new string[](5);
    fieldNames[0] = "programType";
    fieldNames[1] = "maxBoostAmount";
    fieldNames[2] = "grantedBy";
    fieldNames[3] = "grantedAt";
    fieldNames[4] = "isActive";
  }

  /**
   * @notice Register the table with its config.
   */
  function register() internal {
    StoreSwitch.registerTable(_tableId, _fieldLayout, _keySchema, _valueSchema, getKeyNames(), getFieldNames());
  }

  /**
   * @notice Register the table with its config.
   */
  function _register() internal {
    StoreCore.registerTable(_tableId, _fieldLayout, _keySchema, _valueSchema, getKeyNames(), getFieldNames());
  }

  /**
   * @notice Get programType.
   */
  function getProgramType(address programAddress) internal view returns (uint8 programType) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = bytes32(uint256(uint160(programAddress)));

    bytes32 _blob = StoreSwitch.getStaticField(_tableId, _keyTuple, 0, _fieldLayout);
    return (uint8(bytes1(_blob)));
  }

  /**
   * @notice Get programType.
   */
  function _getProgramType(address programAddress) internal view returns (uint8 programType) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = bytes32(uint256(uint160(programAddress)));

    bytes32 _blob = StoreCore.getStaticField(_tableId, _keyTuple, 0, _fieldLayout);
    return (uint8(bytes1(_blob)));
  }

  /**
   * @notice Set programType.
   */
  function setProgramType(address programAddress, uint8 programType) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = bytes32(uint256(uint160(programAddress)));

    StoreSwitch.setStaticField(_tableId, _keyTuple, 0, abi.encodePacked((programType)), _fieldLayout);
  }

  /**
   * @notice Set programType.
   */
  function _setProgramType(address programAddress, uint8 programType) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = bytes32(uint256(uint160(programAddress)));

    StoreCore.setStaticField(_tableId, _keyTuple, 0, abi.encodePacked((programType)), _fieldLayout);
  }

  /**
   * @notice Get maxBoostAmount.
   */
  function getMaxBoostAmount(address programAddress) internal view returns (uint256 maxBoostAmount) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = bytes32(uint256(uint160(programAddress)));

    bytes32 _blob = StoreSwitch.getStaticField(_tableId, _keyTuple, 1, _fieldLayout);
    return (uint256(bytes32(_blob)));
  }

  /**
   * @notice Get maxBoostAmount.
   */
  function _getMaxBoostAmount(address programAddress) internal view returns (uint256 maxBoostAmount) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = bytes32(uint256(uint160(programAddress)));

    bytes32 _blob = StoreCore.getStaticField(_tableId, _keyTuple, 1, _fieldLayout);
    return (uint256(bytes32(_blob)));
  }

  /**
   * @notice Set maxBoostAmount.
   */
  function setMaxBoostAmount(address programAddress, uint256 maxBoostAmount) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = bytes32(uint256(uint160(programAddress)));

    StoreSwitch.setStaticField(_tableId, _keyTuple, 1, abi.encodePacked((maxBoostAmount)), _fieldLayout);
  }

  /**
   * @notice Set maxBoostAmount.
   */
  function _setMaxBoostAmount(address programAddress, uint256 maxBoostAmount) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = bytes32(uint256(uint160(programAddress)));

    StoreCore.setStaticField(_tableId, _keyTuple, 1, abi.encodePacked((maxBoostAmount)), _fieldLayout);
  }

  /**
   * @notice Get grantedBy.
   */
  function getGrantedBy(address programAddress) internal view returns (address grantedBy) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = bytes32(uint256(uint160(programAddress)));

    bytes32 _blob = StoreSwitch.getStaticField(_tableId, _keyTuple, 2, _fieldLayout);
    return (address(bytes20(_blob)));
  }

  /**
   * @notice Get grantedBy.
   */
  function _getGrantedBy(address programAddress) internal view returns (address grantedBy) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = bytes32(uint256(uint160(programAddress)));

    bytes32 _blob = StoreCore.getStaticField(_tableId, _keyTuple, 2, _fieldLayout);
    return (address(bytes20(_blob)));
  }

  /**
   * @notice Set grantedBy.
   */
  function setGrantedBy(address programAddress, address grantedBy) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = bytes32(uint256(uint160(programAddress)));

    StoreSwitch.setStaticField(_tableId, _keyTuple, 2, abi.encodePacked((grantedBy)), _fieldLayout);
  }

  /**
   * @notice Set grantedBy.
   */
  function _setGrantedBy(address programAddress, address grantedBy) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = bytes32(uint256(uint160(programAddress)));

    StoreCore.setStaticField(_tableId, _keyTuple, 2, abi.encodePacked((grantedBy)), _fieldLayout);
  }

  /**
   * @notice Get grantedAt.
   */
  function getGrantedAt(address programAddress) internal view returns (uint64 grantedAt) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = bytes32(uint256(uint160(programAddress)));

    bytes32 _blob = StoreSwitch.getStaticField(_tableId, _keyTuple, 3, _fieldLayout);
    return (uint64(bytes8(_blob)));
  }

  /**
   * @notice Get grantedAt.
   */
  function _getGrantedAt(address programAddress) internal view returns (uint64 grantedAt) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = bytes32(uint256(uint160(programAddress)));

    bytes32 _blob = StoreCore.getStaticField(_tableId, _keyTuple, 3, _fieldLayout);
    return (uint64(bytes8(_blob)));
  }

  /**
   * @notice Set grantedAt.
   */
  function setGrantedAt(address programAddress, uint64 grantedAt) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = bytes32(uint256(uint160(programAddress)));

    StoreSwitch.setStaticField(_tableId, _keyTuple, 3, abi.encodePacked((grantedAt)), _fieldLayout);
  }

  /**
   * @notice Set grantedAt.
   */
  function _setGrantedAt(address programAddress, uint64 grantedAt) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = bytes32(uint256(uint160(programAddress)));

    StoreCore.setStaticField(_tableId, _keyTuple, 3, abi.encodePacked((grantedAt)), _fieldLayout);
  }

  /**
   * @notice Get isActive.
   */
  function getIsActive(address programAddress) internal view returns (uint8 isActive) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = bytes32(uint256(uint160(programAddress)));

    bytes32 _blob = StoreSwitch.getStaticField(_tableId, _keyTuple, 4, _fieldLayout);
    return (uint8(bytes1(_blob)));
  }

  /**
   * @notice Get isActive.
   */
  function _getIsActive(address programAddress) internal view returns (uint8 isActive) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = bytes32(uint256(uint160(programAddress)));

    bytes32 _blob = StoreCore.getStaticField(_tableId, _keyTuple, 4, _fieldLayout);
    return (uint8(bytes1(_blob)));
  }

  /**
   * @notice Set isActive.
   */
  function setIsActive(address programAddress, uint8 isActive) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = bytes32(uint256(uint160(programAddress)));

    StoreSwitch.setStaticField(_tableId, _keyTuple, 4, abi.encodePacked((isActive)), _fieldLayout);
  }

  /**
   * @notice Set isActive.
   */
  function _setIsActive(address programAddress, uint8 isActive) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = bytes32(uint256(uint160(programAddress)));

    StoreCore.setStaticField(_tableId, _keyTuple, 4, abi.encodePacked((isActive)), _fieldLayout);
  }

  /**
   * @notice Get the full data.
   */
  function get(address programAddress) internal view returns (AuthorizedBoosterData memory _table) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = bytes32(uint256(uint160(programAddress)));

    (bytes memory _staticData, EncodedLengths _encodedLengths, bytes memory _dynamicData) = StoreSwitch.getRecord(
      _tableId,
      _keyTuple,
      _fieldLayout
    );
    return decode(_staticData, _encodedLengths, _dynamicData);
  }

  /**
   * @notice Get the full data.
   */
  function _get(address programAddress) internal view returns (AuthorizedBoosterData memory _table) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = bytes32(uint256(uint160(programAddress)));

    (bytes memory _staticData, EncodedLengths _encodedLengths, bytes memory _dynamicData) = StoreCore.getRecord(
      _tableId,
      _keyTuple,
      _fieldLayout
    );
    return decode(_staticData, _encodedLengths, _dynamicData);
  }

  /**
   * @notice Set the full data using individual values.
   */
  function set(
    address programAddress,
    uint8 programType,
    uint256 maxBoostAmount,
    address grantedBy,
    uint64 grantedAt,
    uint8 isActive
  ) internal {
    bytes memory _staticData = encodeStatic(programType, maxBoostAmount, grantedBy, grantedAt, isActive);

    EncodedLengths _encodedLengths;
    bytes memory _dynamicData;

    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = bytes32(uint256(uint160(programAddress)));

    StoreSwitch.setRecord(_tableId, _keyTuple, _staticData, _encodedLengths, _dynamicData);
  }

  /**
   * @notice Set the full data using individual values.
   */
  function _set(
    address programAddress,
    uint8 programType,
    uint256 maxBoostAmount,
    address grantedBy,
    uint64 grantedAt,
    uint8 isActive
  ) internal {
    bytes memory _staticData = encodeStatic(programType, maxBoostAmount, grantedBy, grantedAt, isActive);

    EncodedLengths _encodedLengths;
    bytes memory _dynamicData;

    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = bytes32(uint256(uint160(programAddress)));

    StoreCore.setRecord(_tableId, _keyTuple, _staticData, _encodedLengths, _dynamicData, _fieldLayout);
  }

  /**
   * @notice Set the full data using the data struct.
   */
  function set(address programAddress, AuthorizedBoosterData memory _table) internal {
    bytes memory _staticData = encodeStatic(
      _table.programType,
      _table.maxBoostAmount,
      _table.grantedBy,
      _table.grantedAt,
      _table.isActive
    );

    EncodedLengths _encodedLengths;
    bytes memory _dynamicData;

    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = bytes32(uint256(uint160(programAddress)));

    StoreSwitch.setRecord(_tableId, _keyTuple, _staticData, _encodedLengths, _dynamicData);
  }

  /**
   * @notice Set the full data using the data struct.
   */
  function _set(address programAddress, AuthorizedBoosterData memory _table) internal {
    bytes memory _staticData = encodeStatic(
      _table.programType,
      _table.maxBoostAmount,
      _table.grantedBy,
      _table.grantedAt,
      _table.isActive
    );

    EncodedLengths _encodedLengths;
    bytes memory _dynamicData;

    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = bytes32(uint256(uint160(programAddress)));

    StoreCore.setRecord(_tableId, _keyTuple, _staticData, _encodedLengths, _dynamicData, _fieldLayout);
  }

  /**
   * @notice Decode the tightly packed blob of static data using this table's field layout.
   */
  function decodeStatic(
    bytes memory _blob
  )
    internal
    pure
    returns (uint8 programType, uint256 maxBoostAmount, address grantedBy, uint64 grantedAt, uint8 isActive)
  {
    programType = (uint8(Bytes.getBytes1(_blob, 0)));

    maxBoostAmount = (uint256(Bytes.getBytes32(_blob, 1)));

    grantedBy = (address(Bytes.getBytes20(_blob, 33)));

    grantedAt = (uint64(Bytes.getBytes8(_blob, 53)));

    isActive = (uint8(Bytes.getBytes1(_blob, 61)));
  }

  /**
   * @notice Decode the tightly packed blobs using this table's field layout.
   * @param _staticData Tightly packed static fields.
   *
   *
   */
  function decode(
    bytes memory _staticData,
    EncodedLengths,
    bytes memory
  ) internal pure returns (AuthorizedBoosterData memory _table) {
    (_table.programType, _table.maxBoostAmount, _table.grantedBy, _table.grantedAt, _table.isActive) = decodeStatic(
      _staticData
    );
  }

  /**
   * @notice Delete all data for given keys.
   */
  function deleteRecord(address programAddress) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = bytes32(uint256(uint160(programAddress)));

    StoreSwitch.deleteRecord(_tableId, _keyTuple);
  }

  /**
   * @notice Delete all data for given keys.
   */
  function _deleteRecord(address programAddress) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = bytes32(uint256(uint160(programAddress)));

    StoreCore.deleteRecord(_tableId, _keyTuple, _fieldLayout);
  }

  /**
   * @notice Tightly pack static (fixed length) data using this table's schema.
   * @return The static data, encoded into a sequence of bytes.
   */
  function encodeStatic(
    uint8 programType,
    uint256 maxBoostAmount,
    address grantedBy,
    uint64 grantedAt,
    uint8 isActive
  ) internal pure returns (bytes memory) {
    return abi.encodePacked(programType, maxBoostAmount, grantedBy, grantedAt, isActive);
  }

  /**
   * @notice Encode all of a record's fields.
   * @return The static (fixed length) data, encoded into a sequence of bytes.
   * @return The lengths of the dynamic fields (packed into a single bytes32 value).
   * @return The dynamic (variable length) data, encoded into a sequence of bytes.
   */
  function encode(
    uint8 programType,
    uint256 maxBoostAmount,
    address grantedBy,
    uint64 grantedAt,
    uint8 isActive
  ) internal pure returns (bytes memory, EncodedLengths, bytes memory) {
    bytes memory _staticData = encodeStatic(programType, maxBoostAmount, grantedBy, grantedAt, isActive);

    EncodedLengths _encodedLengths;
    bytes memory _dynamicData;

    return (_staticData, _encodedLengths, _dynamicData);
  }

  /**
   * @notice Encode keys as a bytes32 array using this table's field layout.
   */
  function encodeKeyTuple(address programAddress) internal pure returns (bytes32[] memory) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = bytes32(uint256(uint160(programAddress)));

    return _keyTuple;
  }
}
