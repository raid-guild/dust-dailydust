// SPDX-License-Identifier: MIT
pragma solidity >=0.8.24;

/* Autogenerated file. Do not edit manually. */

import { BoostSystem } from "../../systems/BoostSystem.sol";
import { BoostStatusData } from "../tables/BoostStatus.sol";
import { revertWithBytes } from "@latticexyz/world/src/revertWithBytes.sol";
import { IWorldCall } from "@latticexyz/world/src/IWorldKernel.sol";
import { SystemCall } from "@latticexyz/world/src/SystemCall.sol";
import { WorldContextConsumerLib } from "@latticexyz/world/src/WorldContext.sol";
import { Systems } from "@latticexyz/world/src/codegen/tables/Systems.sol";
import { ResourceId } from "@latticexyz/store/src/ResourceId.sol";
import { StoreSwitch } from "@latticexyz/store/src/StoreSwitch.sol";

type BoostSystemType is bytes32;

// equivalent to WorldResourceIdLib.encode({ typeId: RESOURCE_SYSTEM, namespace: "cms", name: "BoostSystem" }))
BoostSystemType constant boostSystem = BoostSystemType.wrap(
  0x7379636d730000000000000000000000426f6f737453797374656d0000000000
);

struct CallWrapper {
  ResourceId systemId;
  address from;
}

struct RootCallWrapper {
  ResourceId systemId;
  address from;
}

/**
 * @title BoostSystemLib
 * @author MUD (https://mud.dev) by Lattice (https://lattice.xyz)
 * @dev This library is automatically generated from the corresponding system contract. Do not edit manually.
 */
library BoostSystemLib {
  error BoostSystemLib_CallingFromRootSystem();

  function createBoost(
    BoostSystemType self,
    bytes32 articleId,
    address currency,
    uint256 amount,
    uint8 boostType,
    uint64 duration
  ) internal {
    return CallWrapper(self.toResourceId(), address(0)).createBoost(articleId, currency, amount, boostType, duration);
  }

  function deactivateBoost(BoostSystemType self, bytes32 articleId, uint64 startTime) internal {
    return CallWrapper(self.toResourceId(), address(0)).deactivateBoost(articleId, startTime);
  }

  function recordImpression(BoostSystemType self, bytes32 articleId) internal {
    return CallWrapper(self.toResourceId(), address(0)).recordImpression(articleId);
  }

  function recordClick(BoostSystemType self, bytes32 articleId) internal {
    return CallWrapper(self.toResourceId(), address(0)).recordClick(articleId);
  }

  function cleanupExpiredBoost(BoostSystemType self, bytes32 articleId, address booster, uint64 startTime) internal {
    return CallWrapper(self.toResourceId(), address(0)).cleanupExpiredBoost(articleId, booster, startTime);
  }

  function getBoostStatus(
    BoostSystemType self,
    bytes32 articleId
  ) internal view returns (BoostStatusData memory __auxRet0) {
    return CallWrapper(self.toResourceId(), address(0)).getBoostStatus(articleId);
  }

  function isCurrentlyBoosted(BoostSystemType self, bytes32 articleId) internal view returns (bool __auxRet0) {
    return CallWrapper(self.toResourceId(), address(0)).isCurrentlyBoosted(articleId);
  }

  function createBoost(
    CallWrapper memory self,
    bytes32 articleId,
    address currency,
    uint256 amount,
    uint8 boostType,
    uint64 duration
  ) internal {
    // if the contract calling this function is a root system, it should use `callAsRoot`
    if (address(_world()) == address(this)) revert BoostSystemLib_CallingFromRootSystem();

    bytes memory systemCall = abi.encodeCall(
      _createBoost_bytes32_address_uint256_uint8_uint64.createBoost,
      (articleId, currency, amount, boostType, duration)
    );
    self.from == address(0)
      ? _world().call(self.systemId, systemCall)
      : _world().callFrom(self.from, self.systemId, systemCall);
  }

  function deactivateBoost(CallWrapper memory self, bytes32 articleId, uint64 startTime) internal {
    // if the contract calling this function is a root system, it should use `callAsRoot`
    if (address(_world()) == address(this)) revert BoostSystemLib_CallingFromRootSystem();

    bytes memory systemCall = abi.encodeCall(_deactivateBoost_bytes32_uint64.deactivateBoost, (articleId, startTime));
    self.from == address(0)
      ? _world().call(self.systemId, systemCall)
      : _world().callFrom(self.from, self.systemId, systemCall);
  }

  function recordImpression(CallWrapper memory self, bytes32 articleId) internal {
    // if the contract calling this function is a root system, it should use `callAsRoot`
    if (address(_world()) == address(this)) revert BoostSystemLib_CallingFromRootSystem();

    bytes memory systemCall = abi.encodeCall(_recordImpression_bytes32.recordImpression, (articleId));
    self.from == address(0)
      ? _world().call(self.systemId, systemCall)
      : _world().callFrom(self.from, self.systemId, systemCall);
  }

  function recordClick(CallWrapper memory self, bytes32 articleId) internal {
    // if the contract calling this function is a root system, it should use `callAsRoot`
    if (address(_world()) == address(this)) revert BoostSystemLib_CallingFromRootSystem();

    bytes memory systemCall = abi.encodeCall(_recordClick_bytes32.recordClick, (articleId));
    self.from == address(0)
      ? _world().call(self.systemId, systemCall)
      : _world().callFrom(self.from, self.systemId, systemCall);
  }

  function cleanupExpiredBoost(CallWrapper memory self, bytes32 articleId, address booster, uint64 startTime) internal {
    // if the contract calling this function is a root system, it should use `callAsRoot`
    if (address(_world()) == address(this)) revert BoostSystemLib_CallingFromRootSystem();

    bytes memory systemCall = abi.encodeCall(
      _cleanupExpiredBoost_bytes32_address_uint64.cleanupExpiredBoost,
      (articleId, booster, startTime)
    );
    self.from == address(0)
      ? _world().call(self.systemId, systemCall)
      : _world().callFrom(self.from, self.systemId, systemCall);
  }

  function getBoostStatus(
    CallWrapper memory self,
    bytes32 articleId
  ) internal view returns (BoostStatusData memory __auxRet0) {
    // if the contract calling this function is a root system, it should use `callAsRoot`
    if (address(_world()) == address(this)) revert BoostSystemLib_CallingFromRootSystem();

    bytes memory systemCall = abi.encodeCall(_getBoostStatus_bytes32.getBoostStatus, (articleId));
    bytes memory worldCall = self.from == address(0)
      ? abi.encodeCall(IWorldCall.call, (self.systemId, systemCall))
      : abi.encodeCall(IWorldCall.callFrom, (self.from, self.systemId, systemCall));
    (bool success, bytes memory returnData) = address(_world()).staticcall(worldCall);
    if (!success) revertWithBytes(returnData);

    bytes memory result = abi.decode(returnData, (bytes));
    // skip decoding an empty result, which can happen after expectRevert
    if (result.length != 0) {
      return abi.decode(result, (BoostStatusData));
    }
  }

  function isCurrentlyBoosted(CallWrapper memory self, bytes32 articleId) internal view returns (bool __auxRet0) {
    // if the contract calling this function is a root system, it should use `callAsRoot`
    if (address(_world()) == address(this)) revert BoostSystemLib_CallingFromRootSystem();

    bytes memory systemCall = abi.encodeCall(_isCurrentlyBoosted_bytes32.isCurrentlyBoosted, (articleId));
    bytes memory worldCall = self.from == address(0)
      ? abi.encodeCall(IWorldCall.call, (self.systemId, systemCall))
      : abi.encodeCall(IWorldCall.callFrom, (self.from, self.systemId, systemCall));
    (bool success, bytes memory returnData) = address(_world()).staticcall(worldCall);
    if (!success) revertWithBytes(returnData);

    bytes memory result = abi.decode(returnData, (bytes));
    // skip decoding an empty result, which can happen after expectRevert
    if (result.length != 0) {
      return abi.decode(result, (bool));
    }
  }

  function createBoost(
    RootCallWrapper memory self,
    bytes32 articleId,
    address currency,
    uint256 amount,
    uint8 boostType,
    uint64 duration
  ) internal {
    bytes memory systemCall = abi.encodeCall(
      _createBoost_bytes32_address_uint256_uint8_uint64.createBoost,
      (articleId, currency, amount, boostType, duration)
    );
    SystemCall.callWithHooksOrRevert(self.from, self.systemId, systemCall, msg.value);
  }

  function deactivateBoost(RootCallWrapper memory self, bytes32 articleId, uint64 startTime) internal {
    bytes memory systemCall = abi.encodeCall(_deactivateBoost_bytes32_uint64.deactivateBoost, (articleId, startTime));
    SystemCall.callWithHooksOrRevert(self.from, self.systemId, systemCall, msg.value);
  }

  function recordImpression(RootCallWrapper memory self, bytes32 articleId) internal {
    bytes memory systemCall = abi.encodeCall(_recordImpression_bytes32.recordImpression, (articleId));
    SystemCall.callWithHooksOrRevert(self.from, self.systemId, systemCall, msg.value);
  }

  function recordClick(RootCallWrapper memory self, bytes32 articleId) internal {
    bytes memory systemCall = abi.encodeCall(_recordClick_bytes32.recordClick, (articleId));
    SystemCall.callWithHooksOrRevert(self.from, self.systemId, systemCall, msg.value);
  }

  function cleanupExpiredBoost(
    RootCallWrapper memory self,
    bytes32 articleId,
    address booster,
    uint64 startTime
  ) internal {
    bytes memory systemCall = abi.encodeCall(
      _cleanupExpiredBoost_bytes32_address_uint64.cleanupExpiredBoost,
      (articleId, booster, startTime)
    );
    SystemCall.callWithHooksOrRevert(self.from, self.systemId, systemCall, msg.value);
  }

  function getBoostStatus(
    RootCallWrapper memory self,
    bytes32 articleId
  ) internal view returns (BoostStatusData memory __auxRet0) {
    bytes memory systemCall = abi.encodeCall(_getBoostStatus_bytes32.getBoostStatus, (articleId));

    bytes memory result = SystemCall.staticcallOrRevert(self.from, self.systemId, systemCall);
    // skip decoding an empty result, which can happen after expectRevert
    if (result.length != 0) {
      return abi.decode(result, (BoostStatusData));
    }
  }

  function isCurrentlyBoosted(RootCallWrapper memory self, bytes32 articleId) internal view returns (bool __auxRet0) {
    bytes memory systemCall = abi.encodeCall(_isCurrentlyBoosted_bytes32.isCurrentlyBoosted, (articleId));

    bytes memory result = SystemCall.staticcallOrRevert(self.from, self.systemId, systemCall);
    // skip decoding an empty result, which can happen after expectRevert
    if (result.length != 0) {
      return abi.decode(result, (bool));
    }
  }

  function callFrom(BoostSystemType self, address from) internal pure returns (CallWrapper memory) {
    return CallWrapper(self.toResourceId(), from);
  }

  function callAsRoot(BoostSystemType self) internal view returns (RootCallWrapper memory) {
    return RootCallWrapper(self.toResourceId(), WorldContextConsumerLib._msgSender());
  }

  function callAsRootFrom(BoostSystemType self, address from) internal pure returns (RootCallWrapper memory) {
    return RootCallWrapper(self.toResourceId(), from);
  }

  function toResourceId(BoostSystemType self) internal pure returns (ResourceId) {
    return ResourceId.wrap(BoostSystemType.unwrap(self));
  }

  function fromResourceId(ResourceId resourceId) internal pure returns (BoostSystemType) {
    return BoostSystemType.wrap(resourceId.unwrap());
  }

  function getAddress(BoostSystemType self) internal view returns (address) {
    return Systems.getSystem(self.toResourceId());
  }

  function _world() private view returns (IWorldCall) {
    return IWorldCall(StoreSwitch.getStoreAddress());
  }
}

/**
 * System Function Interfaces
 *
 * We generate an interface for each system function, which is then used for encoding system calls.
 * This is necessary to handle function overloading correctly (which abi.encodeCall cannot).
 *
 * Each interface is uniquely named based on the function name and parameters to prevent collisions.
 */

interface _createBoost_bytes32_address_uint256_uint8_uint64 {
  function createBoost(bytes32 articleId, address currency, uint256 amount, uint8 boostType, uint64 duration) external;
}

interface _deactivateBoost_bytes32_uint64 {
  function deactivateBoost(bytes32 articleId, uint64 startTime) external;
}

interface _recordImpression_bytes32 {
  function recordImpression(bytes32 articleId) external;
}

interface _recordClick_bytes32 {
  function recordClick(bytes32 articleId) external;
}

interface _cleanupExpiredBoost_bytes32_address_uint64 {
  function cleanupExpiredBoost(bytes32 articleId, address booster, uint64 startTime) external;
}

interface _getBoostStatus_bytes32 {
  function getBoostStatus(bytes32 articleId) external;
}

interface _isCurrentlyBoosted_bytes32 {
  function isCurrentlyBoosted(bytes32 articleId) external;
}

using BoostSystemLib for BoostSystemType global;
using BoostSystemLib for CallWrapper global;
using BoostSystemLib for RootCallWrapper global;
