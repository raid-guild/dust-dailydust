// SPDX-License-Identifier: MIT
pragma solidity >=0.8.24;

/* Autogenerated file. Do not edit manually. */

import { TagSystem } from "../../systems/TagSystem.sol";
import { revertWithBytes } from "@latticexyz/world/src/revertWithBytes.sol";
import { IWorldCall } from "@latticexyz/world/src/IWorldKernel.sol";
import { SystemCall } from "@latticexyz/world/src/SystemCall.sol";
import { WorldContextConsumerLib } from "@latticexyz/world/src/WorldContext.sol";
import { Systems } from "@latticexyz/world/src/codegen/tables/Systems.sol";
import { ResourceId } from "@latticexyz/store/src/ResourceId.sol";
import { StoreSwitch } from "@latticexyz/store/src/StoreSwitch.sol";

type TagSystemType is bytes32;

// equivalent to WorldResourceIdLib.encode({ typeId: RESOURCE_SYSTEM, namespace: "cms", name: "TagSystem" }))
TagSystemType constant tagSystem = TagSystemType.wrap(
  0x7379636d73000000000000000000000054616753797374656d00000000000000
);

struct CallWrapper {
  ResourceId systemId;
  address from;
}

struct RootCallWrapper {
  ResourceId systemId;
  address from;
}

/**
 * @title TagSystemLib
 * @author MUD (https://mud.dev) by Lattice (https://lattice.xyz)
 * @dev This library is automatically generated from the corresponding system contract. Do not edit manually.
 */
library TagSystemLib {
  error TagSystemLib_CallingFromRootSystem();

  function addTagToArticle(TagSystemType self, bytes32 articleId, string memory tagString) internal {
    return CallWrapper(self.toResourceId(), address(0)).addTagToArticle(articleId, tagString);
  }

  function removeTagFromArticle(TagSystemType self, bytes32 articleId, string memory tagString) internal {
    return CallWrapper(self.toResourceId(), address(0)).removeTagFromArticle(articleId, tagString);
  }

  function addTagsToArticle(TagSystemType self, bytes32 articleId, string[] memory tags) internal {
    return CallWrapper(self.toResourceId(), address(0)).addTagsToArticle(articleId, tags);
  }

  function removeTagsFromArticle(TagSystemType self, bytes32 articleId, string[] memory tags) internal {
    return CallWrapper(self.toResourceId(), address(0)).removeTagsFromArticle(articleId, tags);
  }

  function getTagHash(TagSystemType self, string memory tagString) internal view returns (bytes32 __auxRet0) {
    return CallWrapper(self.toResourceId(), address(0)).getTagHash(tagString);
  }

  function articleHasTag(
    TagSystemType self,
    bytes32 articleId,
    string memory tagString
  ) internal view returns (bool __auxRet0) {
    return CallWrapper(self.toResourceId(), address(0)).articleHasTag(articleId, tagString);
  }

  function tagExists(TagSystemType self, string memory tagString) internal view returns (bool __auxRet0) {
    return CallWrapper(self.toResourceId(), address(0)).tagExists(tagString);
  }

  function getTagByString(TagSystemType self, string memory tagString) internal view returns (string memory __auxRet0) {
    return CallWrapper(self.toResourceId(), address(0)).getTagByString(tagString);
  }

  function addTagToArticle(CallWrapper memory self, bytes32 articleId, string memory tagString) internal {
    // if the contract calling this function is a root system, it should use `callAsRoot`
    if (address(_world()) == address(this)) revert TagSystemLib_CallingFromRootSystem();

    bytes memory systemCall = abi.encodeCall(_addTagToArticle_bytes32_string.addTagToArticle, (articleId, tagString));
    self.from == address(0)
      ? _world().call(self.systemId, systemCall)
      : _world().callFrom(self.from, self.systemId, systemCall);
  }

  function removeTagFromArticle(CallWrapper memory self, bytes32 articleId, string memory tagString) internal {
    // if the contract calling this function is a root system, it should use `callAsRoot`
    if (address(_world()) == address(this)) revert TagSystemLib_CallingFromRootSystem();

    bytes memory systemCall = abi.encodeCall(
      _removeTagFromArticle_bytes32_string.removeTagFromArticle,
      (articleId, tagString)
    );
    self.from == address(0)
      ? _world().call(self.systemId, systemCall)
      : _world().callFrom(self.from, self.systemId, systemCall);
  }

  function addTagsToArticle(CallWrapper memory self, bytes32 articleId, string[] memory tags) internal {
    // if the contract calling this function is a root system, it should use `callAsRoot`
    if (address(_world()) == address(this)) revert TagSystemLib_CallingFromRootSystem();

    bytes memory systemCall = abi.encodeCall(_addTagsToArticle_bytes32_stringArray.addTagsToArticle, (articleId, tags));
    self.from == address(0)
      ? _world().call(self.systemId, systemCall)
      : _world().callFrom(self.from, self.systemId, systemCall);
  }

  function removeTagsFromArticle(CallWrapper memory self, bytes32 articleId, string[] memory tags) internal {
    // if the contract calling this function is a root system, it should use `callAsRoot`
    if (address(_world()) == address(this)) revert TagSystemLib_CallingFromRootSystem();

    bytes memory systemCall = abi.encodeCall(
      _removeTagsFromArticle_bytes32_stringArray.removeTagsFromArticle,
      (articleId, tags)
    );
    self.from == address(0)
      ? _world().call(self.systemId, systemCall)
      : _world().callFrom(self.from, self.systemId, systemCall);
  }

  function getTagHash(CallWrapper memory self, string memory tagString) internal view returns (bytes32 __auxRet0) {
    // if the contract calling this function is a root system, it should use `callAsRoot`
    if (address(_world()) == address(this)) revert TagSystemLib_CallingFromRootSystem();

    bytes memory systemCall = abi.encodeCall(_getTagHash_string.getTagHash, (tagString));
    bytes memory worldCall = self.from == address(0)
      ? abi.encodeCall(IWorldCall.call, (self.systemId, systemCall))
      : abi.encodeCall(IWorldCall.callFrom, (self.from, self.systemId, systemCall));
    (bool success, bytes memory returnData) = address(_world()).staticcall(worldCall);
    if (!success) revertWithBytes(returnData);

    bytes memory result = abi.decode(returnData, (bytes));
    // skip decoding an empty result, which can happen after expectRevert
    if (result.length != 0) {
      return abi.decode(result, (bytes32));
    }
  }

  function articleHasTag(
    CallWrapper memory self,
    bytes32 articleId,
    string memory tagString
  ) internal view returns (bool __auxRet0) {
    // if the contract calling this function is a root system, it should use `callAsRoot`
    if (address(_world()) == address(this)) revert TagSystemLib_CallingFromRootSystem();

    bytes memory systemCall = abi.encodeCall(_articleHasTag_bytes32_string.articleHasTag, (articleId, tagString));
    bytes memory worldCall = self.from == address(0)
      ? abi.encodeCall(IWorldCall.call, (self.systemId, systemCall))
      : abi.encodeCall(IWorldCall.callFrom, (self.from, self.systemId, systemCall));
    (bool success, bytes memory returnData) = address(_world()).staticcall(worldCall);
    if (!success) revertWithBytes(returnData);

    bytes memory result = abi.decode(returnData, (bytes));
    // skip decoding an empty result, which can happen after expectRevert
    if (result.length != 0) {
      return abi.decode(result, (bool));
    }
  }

  function tagExists(CallWrapper memory self, string memory tagString) internal view returns (bool __auxRet0) {
    // if the contract calling this function is a root system, it should use `callAsRoot`
    if (address(_world()) == address(this)) revert TagSystemLib_CallingFromRootSystem();

    bytes memory systemCall = abi.encodeCall(_tagExists_string.tagExists, (tagString));
    bytes memory worldCall = self.from == address(0)
      ? abi.encodeCall(IWorldCall.call, (self.systemId, systemCall))
      : abi.encodeCall(IWorldCall.callFrom, (self.from, self.systemId, systemCall));
    (bool success, bytes memory returnData) = address(_world()).staticcall(worldCall);
    if (!success) revertWithBytes(returnData);

    bytes memory result = abi.decode(returnData, (bytes));
    // skip decoding an empty result, which can happen after expectRevert
    if (result.length != 0) {
      return abi.decode(result, (bool));
    }
  }

  function getTagByString(
    CallWrapper memory self,
    string memory tagString
  ) internal view returns (string memory __auxRet0) {
    // if the contract calling this function is a root system, it should use `callAsRoot`
    if (address(_world()) == address(this)) revert TagSystemLib_CallingFromRootSystem();

    bytes memory systemCall = abi.encodeCall(_getTagByString_string.getTagByString, (tagString));
    bytes memory worldCall = self.from == address(0)
      ? abi.encodeCall(IWorldCall.call, (self.systemId, systemCall))
      : abi.encodeCall(IWorldCall.callFrom, (self.from, self.systemId, systemCall));
    (bool success, bytes memory returnData) = address(_world()).staticcall(worldCall);
    if (!success) revertWithBytes(returnData);

    bytes memory result = abi.decode(returnData, (bytes));
    // skip decoding an empty result, which can happen after expectRevert
    if (result.length != 0) {
      return abi.decode(result, (string));
    }
  }

  function addTagToArticle(RootCallWrapper memory self, bytes32 articleId, string memory tagString) internal {
    bytes memory systemCall = abi.encodeCall(_addTagToArticle_bytes32_string.addTagToArticle, (articleId, tagString));
    SystemCall.callWithHooksOrRevert(self.from, self.systemId, systemCall, msg.value);
  }

  function removeTagFromArticle(RootCallWrapper memory self, bytes32 articleId, string memory tagString) internal {
    bytes memory systemCall = abi.encodeCall(
      _removeTagFromArticle_bytes32_string.removeTagFromArticle,
      (articleId, tagString)
    );
    SystemCall.callWithHooksOrRevert(self.from, self.systemId, systemCall, msg.value);
  }

  function addTagsToArticle(RootCallWrapper memory self, bytes32 articleId, string[] memory tags) internal {
    bytes memory systemCall = abi.encodeCall(_addTagsToArticle_bytes32_stringArray.addTagsToArticle, (articleId, tags));
    SystemCall.callWithHooksOrRevert(self.from, self.systemId, systemCall, msg.value);
  }

  function removeTagsFromArticle(RootCallWrapper memory self, bytes32 articleId, string[] memory tags) internal {
    bytes memory systemCall = abi.encodeCall(
      _removeTagsFromArticle_bytes32_stringArray.removeTagsFromArticle,
      (articleId, tags)
    );
    SystemCall.callWithHooksOrRevert(self.from, self.systemId, systemCall, msg.value);
  }

  function getTagHash(RootCallWrapper memory self, string memory tagString) internal view returns (bytes32 __auxRet0) {
    bytes memory systemCall = abi.encodeCall(_getTagHash_string.getTagHash, (tagString));

    bytes memory result = SystemCall.staticcallOrRevert(self.from, self.systemId, systemCall);
    // skip decoding an empty result, which can happen after expectRevert
    if (result.length != 0) {
      return abi.decode(result, (bytes32));
    }
  }

  function articleHasTag(
    RootCallWrapper memory self,
    bytes32 articleId,
    string memory tagString
  ) internal view returns (bool __auxRet0) {
    bytes memory systemCall = abi.encodeCall(_articleHasTag_bytes32_string.articleHasTag, (articleId, tagString));

    bytes memory result = SystemCall.staticcallOrRevert(self.from, self.systemId, systemCall);
    // skip decoding an empty result, which can happen after expectRevert
    if (result.length != 0) {
      return abi.decode(result, (bool));
    }
  }

  function tagExists(RootCallWrapper memory self, string memory tagString) internal view returns (bool __auxRet0) {
    bytes memory systemCall = abi.encodeCall(_tagExists_string.tagExists, (tagString));

    bytes memory result = SystemCall.staticcallOrRevert(self.from, self.systemId, systemCall);
    // skip decoding an empty result, which can happen after expectRevert
    if (result.length != 0) {
      return abi.decode(result, (bool));
    }
  }

  function getTagByString(
    RootCallWrapper memory self,
    string memory tagString
  ) internal view returns (string memory __auxRet0) {
    bytes memory systemCall = abi.encodeCall(_getTagByString_string.getTagByString, (tagString));

    bytes memory result = SystemCall.staticcallOrRevert(self.from, self.systemId, systemCall);
    // skip decoding an empty result, which can happen after expectRevert
    if (result.length != 0) {
      return abi.decode(result, (string));
    }
  }

  function callFrom(TagSystemType self, address from) internal pure returns (CallWrapper memory) {
    return CallWrapper(self.toResourceId(), from);
  }

  function callAsRoot(TagSystemType self) internal view returns (RootCallWrapper memory) {
    return RootCallWrapper(self.toResourceId(), WorldContextConsumerLib._msgSender());
  }

  function callAsRootFrom(TagSystemType self, address from) internal pure returns (RootCallWrapper memory) {
    return RootCallWrapper(self.toResourceId(), from);
  }

  function toResourceId(TagSystemType self) internal pure returns (ResourceId) {
    return ResourceId.wrap(TagSystemType.unwrap(self));
  }

  function fromResourceId(ResourceId resourceId) internal pure returns (TagSystemType) {
    return TagSystemType.wrap(resourceId.unwrap());
  }

  function getAddress(TagSystemType self) internal view returns (address) {
    return Systems.getSystem(self.toResourceId());
  }

  function _world() private view returns (IWorldCall) {
    return IWorldCall(StoreSwitch.getStoreAddress());
  }
}

/**
 * System Function Interfaces
 *
 * We generate an interface for each system function, which is then used for encoding system calls.
 * This is necessary to handle function overloading correctly (which abi.encodeCall cannot).
 *
 * Each interface is uniquely named based on the function name and parameters to prevent collisions.
 */

interface _addTagToArticle_bytes32_string {
  function addTagToArticle(bytes32 articleId, string memory tagString) external;
}

interface _removeTagFromArticle_bytes32_string {
  function removeTagFromArticle(bytes32 articleId, string memory tagString) external;
}

interface _addTagsToArticle_bytes32_stringArray {
  function addTagsToArticle(bytes32 articleId, string[] memory tags) external;
}

interface _removeTagsFromArticle_bytes32_stringArray {
  function removeTagsFromArticle(bytes32 articleId, string[] memory tags) external;
}

interface _getTagHash_string {
  function getTagHash(string memory tagString) external;
}

interface _articleHasTag_bytes32_string {
  function articleHasTag(bytes32 articleId, string memory tagString) external;
}

interface _tagExists_string {
  function tagExists(string memory tagString) external;
}

interface _getTagByString_string {
  function getTagByString(string memory tagString) external;
}

using TagSystemLib for TagSystemType global;
using TagSystemLib for CallWrapper global;
using TagSystemLib for RootCallWrapper global;
