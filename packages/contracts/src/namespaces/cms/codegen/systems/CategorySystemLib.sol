// SPDX-License-Identifier: MIT
pragma solidity >=0.8.24;

/* Autogenerated file. Do not edit manually. */

import { CategorySystem } from "../../systems/CategorySystem.sol";
import { CategoryData } from "../tables/Category.sol";
import { revertWithBytes } from "@latticexyz/world/src/revertWithBytes.sol";
import { IWorldCall } from "@latticexyz/world/src/IWorldKernel.sol";
import { SystemCall } from "@latticexyz/world/src/SystemCall.sol";
import { WorldContextConsumerLib } from "@latticexyz/world/src/WorldContext.sol";
import { Systems } from "@latticexyz/world/src/codegen/tables/Systems.sol";
import { ResourceId } from "@latticexyz/store/src/ResourceId.sol";
import { StoreSwitch } from "@latticexyz/store/src/StoreSwitch.sol";

type CategorySystemType is bytes32;

// equivalent to WorldResourceIdLib.encode({ typeId: RESOURCE_SYSTEM, namespace: "cms", name: "CategorySystem" }))
CategorySystemType constant categorySystem = CategorySystemType.wrap(
  0x7379636d73000000000000000000000043617465676f727953797374656d0000
);

struct CallWrapper {
  ResourceId systemId;
  address from;
}

struct RootCallWrapper {
  ResourceId systemId;
  address from;
}

/**
 * @title CategorySystemLib
 * @author MUD (https://mud.dev) by Lattice (https://lattice.xyz)
 * @dev This library is automatically generated from the corresponding system contract. Do not edit manually.
 */
library CategorySystemLib {
  error CategorySystemLib_CallingFromRootSystem();

  function createCategory(
    CategorySystemType self,
    string memory categoryName,
    string memory description,
    uint24 color
  ) internal {
    return CallWrapper(self.toResourceId(), address(0)).createCategory(categoryName, description, color);
  }

  function updateCategory(
    CategorySystemType self,
    string memory categoryName,
    string memory newDescription,
    uint24 newColor
  ) internal {
    return CallWrapper(self.toResourceId(), address(0)).updateCategory(categoryName, newDescription, newColor);
  }

  function deleteCategory(CategorySystemType self, string memory categoryName) internal {
    return CallWrapper(self.toResourceId(), address(0)).deleteCategory(categoryName);
  }

  function getCategoryHash(
    CategorySystemType self,
    string memory categoryName
  ) internal view returns (bytes32 __auxRet0) {
    return CallWrapper(self.toResourceId(), address(0)).getCategoryHash(categoryName);
  }

  function getCategoryByName(
    CategorySystemType self,
    string memory categoryName
  ) internal view returns (CategoryData memory __auxRet0) {
    return CallWrapper(self.toResourceId(), address(0)).getCategoryByName(categoryName);
  }

  function categoryExists(CategorySystemType self, string memory categoryName) internal view returns (bool __auxRet0) {
    return CallWrapper(self.toResourceId(), address(0)).categoryExists(categoryName);
  }

  function createCategory(
    CallWrapper memory self,
    string memory categoryName,
    string memory description,
    uint24 color
  ) internal {
    // if the contract calling this function is a root system, it should use `callAsRoot`
    if (address(_world()) == address(this)) revert CategorySystemLib_CallingFromRootSystem();

    bytes memory systemCall = abi.encodeCall(
      _createCategory_string_string_uint24.createCategory,
      (categoryName, description, color)
    );
    self.from == address(0)
      ? _world().call(self.systemId, systemCall)
      : _world().callFrom(self.from, self.systemId, systemCall);
  }

  function updateCategory(
    CallWrapper memory self,
    string memory categoryName,
    string memory newDescription,
    uint24 newColor
  ) internal {
    // if the contract calling this function is a root system, it should use `callAsRoot`
    if (address(_world()) == address(this)) revert CategorySystemLib_CallingFromRootSystem();

    bytes memory systemCall = abi.encodeCall(
      _updateCategory_string_string_uint24.updateCategory,
      (categoryName, newDescription, newColor)
    );
    self.from == address(0)
      ? _world().call(self.systemId, systemCall)
      : _world().callFrom(self.from, self.systemId, systemCall);
  }

  function deleteCategory(CallWrapper memory self, string memory categoryName) internal {
    // if the contract calling this function is a root system, it should use `callAsRoot`
    if (address(_world()) == address(this)) revert CategorySystemLib_CallingFromRootSystem();

    bytes memory systemCall = abi.encodeCall(_deleteCategory_string.deleteCategory, (categoryName));
    self.from == address(0)
      ? _world().call(self.systemId, systemCall)
      : _world().callFrom(self.from, self.systemId, systemCall);
  }

  function getCategoryHash(
    CallWrapper memory self,
    string memory categoryName
  ) internal view returns (bytes32 __auxRet0) {
    // if the contract calling this function is a root system, it should use `callAsRoot`
    if (address(_world()) == address(this)) revert CategorySystemLib_CallingFromRootSystem();

    bytes memory systemCall = abi.encodeCall(_getCategoryHash_string.getCategoryHash, (categoryName));
    bytes memory worldCall = self.from == address(0)
      ? abi.encodeCall(IWorldCall.call, (self.systemId, systemCall))
      : abi.encodeCall(IWorldCall.callFrom, (self.from, self.systemId, systemCall));
    (bool success, bytes memory returnData) = address(_world()).staticcall(worldCall);
    if (!success) revertWithBytes(returnData);

    bytes memory result = abi.decode(returnData, (bytes));
    // skip decoding an empty result, which can happen after expectRevert
    if (result.length != 0) {
      return abi.decode(result, (bytes32));
    }
  }

  function getCategoryByName(
    CallWrapper memory self,
    string memory categoryName
  ) internal view returns (CategoryData memory __auxRet0) {
    // if the contract calling this function is a root system, it should use `callAsRoot`
    if (address(_world()) == address(this)) revert CategorySystemLib_CallingFromRootSystem();

    bytes memory systemCall = abi.encodeCall(_getCategoryByName_string.getCategoryByName, (categoryName));
    bytes memory worldCall = self.from == address(0)
      ? abi.encodeCall(IWorldCall.call, (self.systemId, systemCall))
      : abi.encodeCall(IWorldCall.callFrom, (self.from, self.systemId, systemCall));
    (bool success, bytes memory returnData) = address(_world()).staticcall(worldCall);
    if (!success) revertWithBytes(returnData);

    bytes memory result = abi.decode(returnData, (bytes));
    // skip decoding an empty result, which can happen after expectRevert
    if (result.length != 0) {
      return abi.decode(result, (CategoryData));
    }
  }

  function categoryExists(CallWrapper memory self, string memory categoryName) internal view returns (bool __auxRet0) {
    // if the contract calling this function is a root system, it should use `callAsRoot`
    if (address(_world()) == address(this)) revert CategorySystemLib_CallingFromRootSystem();

    bytes memory systemCall = abi.encodeCall(_categoryExists_string.categoryExists, (categoryName));
    bytes memory worldCall = self.from == address(0)
      ? abi.encodeCall(IWorldCall.call, (self.systemId, systemCall))
      : abi.encodeCall(IWorldCall.callFrom, (self.from, self.systemId, systemCall));
    (bool success, bytes memory returnData) = address(_world()).staticcall(worldCall);
    if (!success) revertWithBytes(returnData);

    bytes memory result = abi.decode(returnData, (bytes));
    // skip decoding an empty result, which can happen after expectRevert
    if (result.length != 0) {
      return abi.decode(result, (bool));
    }
  }

  function createCategory(
    RootCallWrapper memory self,
    string memory categoryName,
    string memory description,
    uint24 color
  ) internal {
    bytes memory systemCall = abi.encodeCall(
      _createCategory_string_string_uint24.createCategory,
      (categoryName, description, color)
    );
    SystemCall.callWithHooksOrRevert(self.from, self.systemId, systemCall, msg.value);
  }

  function updateCategory(
    RootCallWrapper memory self,
    string memory categoryName,
    string memory newDescription,
    uint24 newColor
  ) internal {
    bytes memory systemCall = abi.encodeCall(
      _updateCategory_string_string_uint24.updateCategory,
      (categoryName, newDescription, newColor)
    );
    SystemCall.callWithHooksOrRevert(self.from, self.systemId, systemCall, msg.value);
  }

  function deleteCategory(RootCallWrapper memory self, string memory categoryName) internal {
    bytes memory systemCall = abi.encodeCall(_deleteCategory_string.deleteCategory, (categoryName));
    SystemCall.callWithHooksOrRevert(self.from, self.systemId, systemCall, msg.value);
  }

  function getCategoryHash(
    RootCallWrapper memory self,
    string memory categoryName
  ) internal view returns (bytes32 __auxRet0) {
    bytes memory systemCall = abi.encodeCall(_getCategoryHash_string.getCategoryHash, (categoryName));

    bytes memory result = SystemCall.staticcallOrRevert(self.from, self.systemId, systemCall);
    // skip decoding an empty result, which can happen after expectRevert
    if (result.length != 0) {
      return abi.decode(result, (bytes32));
    }
  }

  function getCategoryByName(
    RootCallWrapper memory self,
    string memory categoryName
  ) internal view returns (CategoryData memory __auxRet0) {
    bytes memory systemCall = abi.encodeCall(_getCategoryByName_string.getCategoryByName, (categoryName));

    bytes memory result = SystemCall.staticcallOrRevert(self.from, self.systemId, systemCall);
    // skip decoding an empty result, which can happen after expectRevert
    if (result.length != 0) {
      return abi.decode(result, (CategoryData));
    }
  }

  function categoryExists(
    RootCallWrapper memory self,
    string memory categoryName
  ) internal view returns (bool __auxRet0) {
    bytes memory systemCall = abi.encodeCall(_categoryExists_string.categoryExists, (categoryName));

    bytes memory result = SystemCall.staticcallOrRevert(self.from, self.systemId, systemCall);
    // skip decoding an empty result, which can happen after expectRevert
    if (result.length != 0) {
      return abi.decode(result, (bool));
    }
  }

  function callFrom(CategorySystemType self, address from) internal pure returns (CallWrapper memory) {
    return CallWrapper(self.toResourceId(), from);
  }

  function callAsRoot(CategorySystemType self) internal view returns (RootCallWrapper memory) {
    return RootCallWrapper(self.toResourceId(), WorldContextConsumerLib._msgSender());
  }

  function callAsRootFrom(CategorySystemType self, address from) internal pure returns (RootCallWrapper memory) {
    return RootCallWrapper(self.toResourceId(), from);
  }

  function toResourceId(CategorySystemType self) internal pure returns (ResourceId) {
    return ResourceId.wrap(CategorySystemType.unwrap(self));
  }

  function fromResourceId(ResourceId resourceId) internal pure returns (CategorySystemType) {
    return CategorySystemType.wrap(resourceId.unwrap());
  }

  function getAddress(CategorySystemType self) internal view returns (address) {
    return Systems.getSystem(self.toResourceId());
  }

  function _world() private view returns (IWorldCall) {
    return IWorldCall(StoreSwitch.getStoreAddress());
  }
}

/**
 * System Function Interfaces
 *
 * We generate an interface for each system function, which is then used for encoding system calls.
 * This is necessary to handle function overloading correctly (which abi.encodeCall cannot).
 *
 * Each interface is uniquely named based on the function name and parameters to prevent collisions.
 */

interface _createCategory_string_string_uint24 {
  function createCategory(string memory categoryName, string memory description, uint24 color) external;
}

interface _updateCategory_string_string_uint24 {
  function updateCategory(string memory categoryName, string memory newDescription, uint24 newColor) external;
}

interface _deleteCategory_string {
  function deleteCategory(string memory categoryName) external;
}

interface _getCategoryHash_string {
  function getCategoryHash(string memory categoryName) external;
}

interface _getCategoryByName_string {
  function getCategoryByName(string memory categoryName) external;
}

interface _categoryExists_string {
  function categoryExists(string memory categoryName) external;
}

using CategorySystemLib for CategorySystemType global;
using CategorySystemLib for CallWrapper global;
using CategorySystemLib for RootCallWrapper global;
