// SPDX-License-Identifier: MIT
pragma solidity >=0.8.24;

/* Autogenerated file. Do not edit manually. */

// Import store internals
import { IStore } from "@latticexyz/store/src/IStore.sol";
import { StoreSwitch } from "@latticexyz/store/src/StoreSwitch.sol";
import { StoreCore } from "@latticexyz/store/src/StoreCore.sol";
import { Bytes } from "@latticexyz/store/src/Bytes.sol";
import { Memory } from "@latticexyz/store/src/Memory.sol";
import { SliceLib } from "@latticexyz/store/src/Slice.sol";
import { EncodeArray } from "@latticexyz/store/src/tightcoder/EncodeArray.sol";
import { FieldLayout } from "@latticexyz/store/src/FieldLayout.sol";
import { Schema } from "@latticexyz/store/src/Schema.sol";
import { EncodedLengths, EncodedLengthsLib } from "@latticexyz/store/src/EncodedLengths.sol";
import { ResourceId } from "@latticexyz/store/src/ResourceId.sol";

struct BoostStatusData {
  uint256 totalBoostAmount;
  uint64 boostUntil;
  uint16 activeBoostCount;
  uint8 isCurrentlyBoosted;
}

library BoostStatus {
  // Hex below is the result of `WorldResourceIdLib.encode({ namespace: "cms", name: "BoostStatus", typeId: RESOURCE_TABLE });`
  ResourceId constant _tableId = ResourceId.wrap(0x7462636d730000000000000000000000426f6f73745374617475730000000000);

  FieldLayout constant _fieldLayout =
    FieldLayout.wrap(0x002b040020080201000000000000000000000000000000000000000000000000);

  // Hex-encoded key schema of (bytes32)
  Schema constant _keySchema = Schema.wrap(0x002001005f000000000000000000000000000000000000000000000000000000);
  // Hex-encoded value schema of (uint256, uint64, uint16, uint8)
  Schema constant _valueSchema = Schema.wrap(0x002b04001f070100000000000000000000000000000000000000000000000000);

  /**
   * @notice Get the table's key field names.
   * @return keyNames An array of strings with the names of key fields.
   */
  function getKeyNames() internal pure returns (string[] memory keyNames) {
    keyNames = new string[](1);
    keyNames[0] = "articleId";
  }

  /**
   * @notice Get the table's value field names.
   * @return fieldNames An array of strings with the names of value fields.
   */
  function getFieldNames() internal pure returns (string[] memory fieldNames) {
    fieldNames = new string[](4);
    fieldNames[0] = "totalBoostAmount";
    fieldNames[1] = "boostUntil";
    fieldNames[2] = "activeBoostCount";
    fieldNames[3] = "isCurrentlyBoosted";
  }

  /**
   * @notice Register the table with its config.
   */
  function register() internal {
    StoreSwitch.registerTable(_tableId, _fieldLayout, _keySchema, _valueSchema, getKeyNames(), getFieldNames());
  }

  /**
   * @notice Register the table with its config.
   */
  function _register() internal {
    StoreCore.registerTable(_tableId, _fieldLayout, _keySchema, _valueSchema, getKeyNames(), getFieldNames());
  }

  /**
   * @notice Get totalBoostAmount.
   */
  function getTotalBoostAmount(bytes32 articleId) internal view returns (uint256 totalBoostAmount) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    bytes32 _blob = StoreSwitch.getStaticField(_tableId, _keyTuple, 0, _fieldLayout);
    return (uint256(bytes32(_blob)));
  }

  /**
   * @notice Get totalBoostAmount.
   */
  function _getTotalBoostAmount(bytes32 articleId) internal view returns (uint256 totalBoostAmount) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    bytes32 _blob = StoreCore.getStaticField(_tableId, _keyTuple, 0, _fieldLayout);
    return (uint256(bytes32(_blob)));
  }

  /**
   * @notice Set totalBoostAmount.
   */
  function setTotalBoostAmount(bytes32 articleId, uint256 totalBoostAmount) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    StoreSwitch.setStaticField(_tableId, _keyTuple, 0, abi.encodePacked((totalBoostAmount)), _fieldLayout);
  }

  /**
   * @notice Set totalBoostAmount.
   */
  function _setTotalBoostAmount(bytes32 articleId, uint256 totalBoostAmount) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    StoreCore.setStaticField(_tableId, _keyTuple, 0, abi.encodePacked((totalBoostAmount)), _fieldLayout);
  }

  /**
   * @notice Get boostUntil.
   */
  function getBoostUntil(bytes32 articleId) internal view returns (uint64 boostUntil) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    bytes32 _blob = StoreSwitch.getStaticField(_tableId, _keyTuple, 1, _fieldLayout);
    return (uint64(bytes8(_blob)));
  }

  /**
   * @notice Get boostUntil.
   */
  function _getBoostUntil(bytes32 articleId) internal view returns (uint64 boostUntil) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    bytes32 _blob = StoreCore.getStaticField(_tableId, _keyTuple, 1, _fieldLayout);
    return (uint64(bytes8(_blob)));
  }

  /**
   * @notice Set boostUntil.
   */
  function setBoostUntil(bytes32 articleId, uint64 boostUntil) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    StoreSwitch.setStaticField(_tableId, _keyTuple, 1, abi.encodePacked((boostUntil)), _fieldLayout);
  }

  /**
   * @notice Set boostUntil.
   */
  function _setBoostUntil(bytes32 articleId, uint64 boostUntil) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    StoreCore.setStaticField(_tableId, _keyTuple, 1, abi.encodePacked((boostUntil)), _fieldLayout);
  }

  /**
   * @notice Get activeBoostCount.
   */
  function getActiveBoostCount(bytes32 articleId) internal view returns (uint16 activeBoostCount) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    bytes32 _blob = StoreSwitch.getStaticField(_tableId, _keyTuple, 2, _fieldLayout);
    return (uint16(bytes2(_blob)));
  }

  /**
   * @notice Get activeBoostCount.
   */
  function _getActiveBoostCount(bytes32 articleId) internal view returns (uint16 activeBoostCount) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    bytes32 _blob = StoreCore.getStaticField(_tableId, _keyTuple, 2, _fieldLayout);
    return (uint16(bytes2(_blob)));
  }

  /**
   * @notice Set activeBoostCount.
   */
  function setActiveBoostCount(bytes32 articleId, uint16 activeBoostCount) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    StoreSwitch.setStaticField(_tableId, _keyTuple, 2, abi.encodePacked((activeBoostCount)), _fieldLayout);
  }

  /**
   * @notice Set activeBoostCount.
   */
  function _setActiveBoostCount(bytes32 articleId, uint16 activeBoostCount) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    StoreCore.setStaticField(_tableId, _keyTuple, 2, abi.encodePacked((activeBoostCount)), _fieldLayout);
  }

  /**
   * @notice Get isCurrentlyBoosted.
   */
  function getIsCurrentlyBoosted(bytes32 articleId) internal view returns (uint8 isCurrentlyBoosted) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    bytes32 _blob = StoreSwitch.getStaticField(_tableId, _keyTuple, 3, _fieldLayout);
    return (uint8(bytes1(_blob)));
  }

  /**
   * @notice Get isCurrentlyBoosted.
   */
  function _getIsCurrentlyBoosted(bytes32 articleId) internal view returns (uint8 isCurrentlyBoosted) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    bytes32 _blob = StoreCore.getStaticField(_tableId, _keyTuple, 3, _fieldLayout);
    return (uint8(bytes1(_blob)));
  }

  /**
   * @notice Set isCurrentlyBoosted.
   */
  function setIsCurrentlyBoosted(bytes32 articleId, uint8 isCurrentlyBoosted) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    StoreSwitch.setStaticField(_tableId, _keyTuple, 3, abi.encodePacked((isCurrentlyBoosted)), _fieldLayout);
  }

  /**
   * @notice Set isCurrentlyBoosted.
   */
  function _setIsCurrentlyBoosted(bytes32 articleId, uint8 isCurrentlyBoosted) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    StoreCore.setStaticField(_tableId, _keyTuple, 3, abi.encodePacked((isCurrentlyBoosted)), _fieldLayout);
  }

  /**
   * @notice Get the full data.
   */
  function get(bytes32 articleId) internal view returns (BoostStatusData memory _table) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    (bytes memory _staticData, EncodedLengths _encodedLengths, bytes memory _dynamicData) = StoreSwitch.getRecord(
      _tableId,
      _keyTuple,
      _fieldLayout
    );
    return decode(_staticData, _encodedLengths, _dynamicData);
  }

  /**
   * @notice Get the full data.
   */
  function _get(bytes32 articleId) internal view returns (BoostStatusData memory _table) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    (bytes memory _staticData, EncodedLengths _encodedLengths, bytes memory _dynamicData) = StoreCore.getRecord(
      _tableId,
      _keyTuple,
      _fieldLayout
    );
    return decode(_staticData, _encodedLengths, _dynamicData);
  }

  /**
   * @notice Set the full data using individual values.
   */
  function set(
    bytes32 articleId,
    uint256 totalBoostAmount,
    uint64 boostUntil,
    uint16 activeBoostCount,
    uint8 isCurrentlyBoosted
  ) internal {
    bytes memory _staticData = encodeStatic(totalBoostAmount, boostUntil, activeBoostCount, isCurrentlyBoosted);

    EncodedLengths _encodedLengths;
    bytes memory _dynamicData;

    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    StoreSwitch.setRecord(_tableId, _keyTuple, _staticData, _encodedLengths, _dynamicData);
  }

  /**
   * @notice Set the full data using individual values.
   */
  function _set(
    bytes32 articleId,
    uint256 totalBoostAmount,
    uint64 boostUntil,
    uint16 activeBoostCount,
    uint8 isCurrentlyBoosted
  ) internal {
    bytes memory _staticData = encodeStatic(totalBoostAmount, boostUntil, activeBoostCount, isCurrentlyBoosted);

    EncodedLengths _encodedLengths;
    bytes memory _dynamicData;

    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    StoreCore.setRecord(_tableId, _keyTuple, _staticData, _encodedLengths, _dynamicData, _fieldLayout);
  }

  /**
   * @notice Set the full data using the data struct.
   */
  function set(bytes32 articleId, BoostStatusData memory _table) internal {
    bytes memory _staticData = encodeStatic(
      _table.totalBoostAmount,
      _table.boostUntil,
      _table.activeBoostCount,
      _table.isCurrentlyBoosted
    );

    EncodedLengths _encodedLengths;
    bytes memory _dynamicData;

    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    StoreSwitch.setRecord(_tableId, _keyTuple, _staticData, _encodedLengths, _dynamicData);
  }

  /**
   * @notice Set the full data using the data struct.
   */
  function _set(bytes32 articleId, BoostStatusData memory _table) internal {
    bytes memory _staticData = encodeStatic(
      _table.totalBoostAmount,
      _table.boostUntil,
      _table.activeBoostCount,
      _table.isCurrentlyBoosted
    );

    EncodedLengths _encodedLengths;
    bytes memory _dynamicData;

    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    StoreCore.setRecord(_tableId, _keyTuple, _staticData, _encodedLengths, _dynamicData, _fieldLayout);
  }

  /**
   * @notice Decode the tightly packed blob of static data using this table's field layout.
   */
  function decodeStatic(
    bytes memory _blob
  )
    internal
    pure
    returns (uint256 totalBoostAmount, uint64 boostUntil, uint16 activeBoostCount, uint8 isCurrentlyBoosted)
  {
    totalBoostAmount = (uint256(Bytes.getBytes32(_blob, 0)));

    boostUntil = (uint64(Bytes.getBytes8(_blob, 32)));

    activeBoostCount = (uint16(Bytes.getBytes2(_blob, 40)));

    isCurrentlyBoosted = (uint8(Bytes.getBytes1(_blob, 42)));
  }

  /**
   * @notice Decode the tightly packed blobs using this table's field layout.
   * @param _staticData Tightly packed static fields.
   *
   *
   */
  function decode(
    bytes memory _staticData,
    EncodedLengths,
    bytes memory
  ) internal pure returns (BoostStatusData memory _table) {
    (_table.totalBoostAmount, _table.boostUntil, _table.activeBoostCount, _table.isCurrentlyBoosted) = decodeStatic(
      _staticData
    );
  }

  /**
   * @notice Delete all data for given keys.
   */
  function deleteRecord(bytes32 articleId) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    StoreSwitch.deleteRecord(_tableId, _keyTuple);
  }

  /**
   * @notice Delete all data for given keys.
   */
  function _deleteRecord(bytes32 articleId) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    StoreCore.deleteRecord(_tableId, _keyTuple, _fieldLayout);
  }

  /**
   * @notice Tightly pack static (fixed length) data using this table's schema.
   * @return The static data, encoded into a sequence of bytes.
   */
  function encodeStatic(
    uint256 totalBoostAmount,
    uint64 boostUntil,
    uint16 activeBoostCount,
    uint8 isCurrentlyBoosted
  ) internal pure returns (bytes memory) {
    return abi.encodePacked(totalBoostAmount, boostUntil, activeBoostCount, isCurrentlyBoosted);
  }

  /**
   * @notice Encode all of a record's fields.
   * @return The static (fixed length) data, encoded into a sequence of bytes.
   * @return The lengths of the dynamic fields (packed into a single bytes32 value).
   * @return The dynamic (variable length) data, encoded into a sequence of bytes.
   */
  function encode(
    uint256 totalBoostAmount,
    uint64 boostUntil,
    uint16 activeBoostCount,
    uint8 isCurrentlyBoosted
  ) internal pure returns (bytes memory, EncodedLengths, bytes memory) {
    bytes memory _staticData = encodeStatic(totalBoostAmount, boostUntil, activeBoostCount, isCurrentlyBoosted);

    EncodedLengths _encodedLengths;
    bytes memory _dynamicData;

    return (_staticData, _encodedLengths, _dynamicData);
  }

  /**
   * @notice Encode keys as a bytes32 array using this table's field layout.
   */
  function encodeKeyTuple(bytes32 articleId) internal pure returns (bytes32[] memory) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    return _keyTuple;
  }
}
