// SPDX-License-Identifier: MIT
pragma solidity >=0.8.24;

/* Autogenerated file. Do not edit manually. */

// Import store internals
import { IStore } from "@latticexyz/store/src/IStore.sol";
import { StoreSwitch } from "@latticexyz/store/src/StoreSwitch.sol";
import { StoreCore } from "@latticexyz/store/src/StoreCore.sol";
import { Bytes } from "@latticexyz/store/src/Bytes.sol";
import { Memory } from "@latticexyz/store/src/Memory.sol";
import { SliceLib } from "@latticexyz/store/src/Slice.sol";
import { EncodeArray } from "@latticexyz/store/src/tightcoder/EncodeArray.sol";
import { FieldLayout } from "@latticexyz/store/src/FieldLayout.sol";
import { Schema } from "@latticexyz/store/src/Schema.sol";
import { EncodedLengths, EncodedLengthsLib } from "@latticexyz/store/src/EncodedLengths.sol";
import { ResourceId } from "@latticexyz/store/src/ResourceId.sol";

struct ArticleData {
  address owner;
  uint64 createdAt;
  uint64 updatedAt;
  uint8 articleType;
  bytes32 category;
  string title;
  string kicker;
  string content;
  string headerImageUrl;
}

library Article {
  // Hex below is the result of `WorldResourceIdLib.encode({ namespace: "cms", name: "Article", typeId: RESOURCE_TABLE });`
  ResourceId constant _tableId = ResourceId.wrap(0x7462636d73000000000000000000000041727469636c65000000000000000000);

  FieldLayout constant _fieldLayout =
    FieldLayout.wrap(0x0045050414080801200000000000000000000000000000000000000000000000);

  // Hex-encoded key schema of (bytes32)
  Schema constant _keySchema = Schema.wrap(0x002001005f000000000000000000000000000000000000000000000000000000);
  // Hex-encoded value schema of (address, uint64, uint64, uint8, bytes32, string, string, string, string)
  Schema constant _valueSchema = Schema.wrap(0x00450504610707005fc5c5c5c500000000000000000000000000000000000000);

  /**
   * @notice Get the table's key field names.
   * @return keyNames An array of strings with the names of key fields.
   */
  function getKeyNames() internal pure returns (string[] memory keyNames) {
    keyNames = new string[](1);
    keyNames[0] = "articleId";
  }

  /**
   * @notice Get the table's value field names.
   * @return fieldNames An array of strings with the names of value fields.
   */
  function getFieldNames() internal pure returns (string[] memory fieldNames) {
    fieldNames = new string[](9);
    fieldNames[0] = "owner";
    fieldNames[1] = "createdAt";
    fieldNames[2] = "updatedAt";
    fieldNames[3] = "articleType";
    fieldNames[4] = "category";
    fieldNames[5] = "title";
    fieldNames[6] = "kicker";
    fieldNames[7] = "content";
    fieldNames[8] = "headerImageUrl";
  }

  /**
   * @notice Register the table with its config.
   */
  function register() internal {
    StoreSwitch.registerTable(_tableId, _fieldLayout, _keySchema, _valueSchema, getKeyNames(), getFieldNames());
  }

  /**
   * @notice Register the table with its config.
   */
  function _register() internal {
    StoreCore.registerTable(_tableId, _fieldLayout, _keySchema, _valueSchema, getKeyNames(), getFieldNames());
  }

  /**
   * @notice Get owner.
   */
  function getOwner(bytes32 articleId) internal view returns (address owner) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    bytes32 _blob = StoreSwitch.getStaticField(_tableId, _keyTuple, 0, _fieldLayout);
    return (address(bytes20(_blob)));
  }

  /**
   * @notice Get owner.
   */
  function _getOwner(bytes32 articleId) internal view returns (address owner) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    bytes32 _blob = StoreCore.getStaticField(_tableId, _keyTuple, 0, _fieldLayout);
    return (address(bytes20(_blob)));
  }

  /**
   * @notice Set owner.
   */
  function setOwner(bytes32 articleId, address owner) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    StoreSwitch.setStaticField(_tableId, _keyTuple, 0, abi.encodePacked((owner)), _fieldLayout);
  }

  /**
   * @notice Set owner.
   */
  function _setOwner(bytes32 articleId, address owner) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    StoreCore.setStaticField(_tableId, _keyTuple, 0, abi.encodePacked((owner)), _fieldLayout);
  }

  /**
   * @notice Get createdAt.
   */
  function getCreatedAt(bytes32 articleId) internal view returns (uint64 createdAt) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    bytes32 _blob = StoreSwitch.getStaticField(_tableId, _keyTuple, 1, _fieldLayout);
    return (uint64(bytes8(_blob)));
  }

  /**
   * @notice Get createdAt.
   */
  function _getCreatedAt(bytes32 articleId) internal view returns (uint64 createdAt) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    bytes32 _blob = StoreCore.getStaticField(_tableId, _keyTuple, 1, _fieldLayout);
    return (uint64(bytes8(_blob)));
  }

  /**
   * @notice Set createdAt.
   */
  function setCreatedAt(bytes32 articleId, uint64 createdAt) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    StoreSwitch.setStaticField(_tableId, _keyTuple, 1, abi.encodePacked((createdAt)), _fieldLayout);
  }

  /**
   * @notice Set createdAt.
   */
  function _setCreatedAt(bytes32 articleId, uint64 createdAt) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    StoreCore.setStaticField(_tableId, _keyTuple, 1, abi.encodePacked((createdAt)), _fieldLayout);
  }

  /**
   * @notice Get updatedAt.
   */
  function getUpdatedAt(bytes32 articleId) internal view returns (uint64 updatedAt) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    bytes32 _blob = StoreSwitch.getStaticField(_tableId, _keyTuple, 2, _fieldLayout);
    return (uint64(bytes8(_blob)));
  }

  /**
   * @notice Get updatedAt.
   */
  function _getUpdatedAt(bytes32 articleId) internal view returns (uint64 updatedAt) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    bytes32 _blob = StoreCore.getStaticField(_tableId, _keyTuple, 2, _fieldLayout);
    return (uint64(bytes8(_blob)));
  }

  /**
   * @notice Set updatedAt.
   */
  function setUpdatedAt(bytes32 articleId, uint64 updatedAt) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    StoreSwitch.setStaticField(_tableId, _keyTuple, 2, abi.encodePacked((updatedAt)), _fieldLayout);
  }

  /**
   * @notice Set updatedAt.
   */
  function _setUpdatedAt(bytes32 articleId, uint64 updatedAt) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    StoreCore.setStaticField(_tableId, _keyTuple, 2, abi.encodePacked((updatedAt)), _fieldLayout);
  }

  /**
   * @notice Get articleType.
   */
  function getArticleType(bytes32 articleId) internal view returns (uint8 articleType) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    bytes32 _blob = StoreSwitch.getStaticField(_tableId, _keyTuple, 3, _fieldLayout);
    return (uint8(bytes1(_blob)));
  }

  /**
   * @notice Get articleType.
   */
  function _getArticleType(bytes32 articleId) internal view returns (uint8 articleType) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    bytes32 _blob = StoreCore.getStaticField(_tableId, _keyTuple, 3, _fieldLayout);
    return (uint8(bytes1(_blob)));
  }

  /**
   * @notice Set articleType.
   */
  function setArticleType(bytes32 articleId, uint8 articleType) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    StoreSwitch.setStaticField(_tableId, _keyTuple, 3, abi.encodePacked((articleType)), _fieldLayout);
  }

  /**
   * @notice Set articleType.
   */
  function _setArticleType(bytes32 articleId, uint8 articleType) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    StoreCore.setStaticField(_tableId, _keyTuple, 3, abi.encodePacked((articleType)), _fieldLayout);
  }

  /**
   * @notice Get category.
   */
  function getCategory(bytes32 articleId) internal view returns (bytes32 category) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    bytes32 _blob = StoreSwitch.getStaticField(_tableId, _keyTuple, 4, _fieldLayout);
    return (bytes32(_blob));
  }

  /**
   * @notice Get category.
   */
  function _getCategory(bytes32 articleId) internal view returns (bytes32 category) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    bytes32 _blob = StoreCore.getStaticField(_tableId, _keyTuple, 4, _fieldLayout);
    return (bytes32(_blob));
  }

  /**
   * @notice Set category.
   */
  function setCategory(bytes32 articleId, bytes32 category) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    StoreSwitch.setStaticField(_tableId, _keyTuple, 4, abi.encodePacked((category)), _fieldLayout);
  }

  /**
   * @notice Set category.
   */
  function _setCategory(bytes32 articleId, bytes32 category) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    StoreCore.setStaticField(_tableId, _keyTuple, 4, abi.encodePacked((category)), _fieldLayout);
  }

  /**
   * @notice Get title.
   */
  function getTitle(bytes32 articleId) internal view returns (string memory title) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    bytes memory _blob = StoreSwitch.getDynamicField(_tableId, _keyTuple, 0);
    return (string(_blob));
  }

  /**
   * @notice Get title.
   */
  function _getTitle(bytes32 articleId) internal view returns (string memory title) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    bytes memory _blob = StoreCore.getDynamicField(_tableId, _keyTuple, 0);
    return (string(_blob));
  }

  /**
   * @notice Set title.
   */
  function setTitle(bytes32 articleId, string memory title) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    StoreSwitch.setDynamicField(_tableId, _keyTuple, 0, bytes((title)));
  }

  /**
   * @notice Set title.
   */
  function _setTitle(bytes32 articleId, string memory title) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    StoreCore.setDynamicField(_tableId, _keyTuple, 0, bytes((title)));
  }

  /**
   * @notice Get the length of title.
   */
  function lengthTitle(bytes32 articleId) internal view returns (uint256) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    uint256 _byteLength = StoreSwitch.getDynamicFieldLength(_tableId, _keyTuple, 0);
    unchecked {
      return _byteLength / 1;
    }
  }

  /**
   * @notice Get the length of title.
   */
  function _lengthTitle(bytes32 articleId) internal view returns (uint256) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    uint256 _byteLength = StoreCore.getDynamicFieldLength(_tableId, _keyTuple, 0);
    unchecked {
      return _byteLength / 1;
    }
  }

  /**
   * @notice Get an item of title.
   * @dev Reverts with Store_IndexOutOfBounds if `_index` is out of bounds for the array.
   */
  function getItemTitle(bytes32 articleId, uint256 _index) internal view returns (string memory) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    unchecked {
      bytes memory _blob = StoreSwitch.getDynamicFieldSlice(_tableId, _keyTuple, 0, _index * 1, (_index + 1) * 1);
      return (string(_blob));
    }
  }

  /**
   * @notice Get an item of title.
   * @dev Reverts with Store_IndexOutOfBounds if `_index` is out of bounds for the array.
   */
  function _getItemTitle(bytes32 articleId, uint256 _index) internal view returns (string memory) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    unchecked {
      bytes memory _blob = StoreCore.getDynamicFieldSlice(_tableId, _keyTuple, 0, _index * 1, (_index + 1) * 1);
      return (string(_blob));
    }
  }

  /**
   * @notice Push a slice to title.
   */
  function pushTitle(bytes32 articleId, string memory _slice) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    StoreSwitch.pushToDynamicField(_tableId, _keyTuple, 0, bytes((_slice)));
  }

  /**
   * @notice Push a slice to title.
   */
  function _pushTitle(bytes32 articleId, string memory _slice) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    StoreCore.pushToDynamicField(_tableId, _keyTuple, 0, bytes((_slice)));
  }

  /**
   * @notice Pop a slice from title.
   */
  function popTitle(bytes32 articleId) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    StoreSwitch.popFromDynamicField(_tableId, _keyTuple, 0, 1);
  }

  /**
   * @notice Pop a slice from title.
   */
  function _popTitle(bytes32 articleId) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    StoreCore.popFromDynamicField(_tableId, _keyTuple, 0, 1);
  }

  /**
   * @notice Update a slice of title at `_index`.
   */
  function updateTitle(bytes32 articleId, uint256 _index, string memory _slice) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    unchecked {
      bytes memory _encoded = bytes((_slice));
      StoreSwitch.spliceDynamicData(_tableId, _keyTuple, 0, uint40(_index * 1), uint40(_encoded.length), _encoded);
    }
  }

  /**
   * @notice Update a slice of title at `_index`.
   */
  function _updateTitle(bytes32 articleId, uint256 _index, string memory _slice) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    unchecked {
      bytes memory _encoded = bytes((_slice));
      StoreCore.spliceDynamicData(_tableId, _keyTuple, 0, uint40(_index * 1), uint40(_encoded.length), _encoded);
    }
  }

  /**
   * @notice Get kicker.
   */
  function getKicker(bytes32 articleId) internal view returns (string memory kicker) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    bytes memory _blob = StoreSwitch.getDynamicField(_tableId, _keyTuple, 1);
    return (string(_blob));
  }

  /**
   * @notice Get kicker.
   */
  function _getKicker(bytes32 articleId) internal view returns (string memory kicker) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    bytes memory _blob = StoreCore.getDynamicField(_tableId, _keyTuple, 1);
    return (string(_blob));
  }

  /**
   * @notice Set kicker.
   */
  function setKicker(bytes32 articleId, string memory kicker) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    StoreSwitch.setDynamicField(_tableId, _keyTuple, 1, bytes((kicker)));
  }

  /**
   * @notice Set kicker.
   */
  function _setKicker(bytes32 articleId, string memory kicker) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    StoreCore.setDynamicField(_tableId, _keyTuple, 1, bytes((kicker)));
  }

  /**
   * @notice Get the length of kicker.
   */
  function lengthKicker(bytes32 articleId) internal view returns (uint256) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    uint256 _byteLength = StoreSwitch.getDynamicFieldLength(_tableId, _keyTuple, 1);
    unchecked {
      return _byteLength / 1;
    }
  }

  /**
   * @notice Get the length of kicker.
   */
  function _lengthKicker(bytes32 articleId) internal view returns (uint256) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    uint256 _byteLength = StoreCore.getDynamicFieldLength(_tableId, _keyTuple, 1);
    unchecked {
      return _byteLength / 1;
    }
  }

  /**
   * @notice Get an item of kicker.
   * @dev Reverts with Store_IndexOutOfBounds if `_index` is out of bounds for the array.
   */
  function getItemKicker(bytes32 articleId, uint256 _index) internal view returns (string memory) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    unchecked {
      bytes memory _blob = StoreSwitch.getDynamicFieldSlice(_tableId, _keyTuple, 1, _index * 1, (_index + 1) * 1);
      return (string(_blob));
    }
  }

  /**
   * @notice Get an item of kicker.
   * @dev Reverts with Store_IndexOutOfBounds if `_index` is out of bounds for the array.
   */
  function _getItemKicker(bytes32 articleId, uint256 _index) internal view returns (string memory) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    unchecked {
      bytes memory _blob = StoreCore.getDynamicFieldSlice(_tableId, _keyTuple, 1, _index * 1, (_index + 1) * 1);
      return (string(_blob));
    }
  }

  /**
   * @notice Push a slice to kicker.
   */
  function pushKicker(bytes32 articleId, string memory _slice) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    StoreSwitch.pushToDynamicField(_tableId, _keyTuple, 1, bytes((_slice)));
  }

  /**
   * @notice Push a slice to kicker.
   */
  function _pushKicker(bytes32 articleId, string memory _slice) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    StoreCore.pushToDynamicField(_tableId, _keyTuple, 1, bytes((_slice)));
  }

  /**
   * @notice Pop a slice from kicker.
   */
  function popKicker(bytes32 articleId) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    StoreSwitch.popFromDynamicField(_tableId, _keyTuple, 1, 1);
  }

  /**
   * @notice Pop a slice from kicker.
   */
  function _popKicker(bytes32 articleId) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    StoreCore.popFromDynamicField(_tableId, _keyTuple, 1, 1);
  }

  /**
   * @notice Update a slice of kicker at `_index`.
   */
  function updateKicker(bytes32 articleId, uint256 _index, string memory _slice) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    unchecked {
      bytes memory _encoded = bytes((_slice));
      StoreSwitch.spliceDynamicData(_tableId, _keyTuple, 1, uint40(_index * 1), uint40(_encoded.length), _encoded);
    }
  }

  /**
   * @notice Update a slice of kicker at `_index`.
   */
  function _updateKicker(bytes32 articleId, uint256 _index, string memory _slice) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    unchecked {
      bytes memory _encoded = bytes((_slice));
      StoreCore.spliceDynamicData(_tableId, _keyTuple, 1, uint40(_index * 1), uint40(_encoded.length), _encoded);
    }
  }

  /**
   * @notice Get content.
   */
  function getContent(bytes32 articleId) internal view returns (string memory content) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    bytes memory _blob = StoreSwitch.getDynamicField(_tableId, _keyTuple, 2);
    return (string(_blob));
  }

  /**
   * @notice Get content.
   */
  function _getContent(bytes32 articleId) internal view returns (string memory content) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    bytes memory _blob = StoreCore.getDynamicField(_tableId, _keyTuple, 2);
    return (string(_blob));
  }

  /**
   * @notice Set content.
   */
  function setContent(bytes32 articleId, string memory content) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    StoreSwitch.setDynamicField(_tableId, _keyTuple, 2, bytes((content)));
  }

  /**
   * @notice Set content.
   */
  function _setContent(bytes32 articleId, string memory content) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    StoreCore.setDynamicField(_tableId, _keyTuple, 2, bytes((content)));
  }

  /**
   * @notice Get the length of content.
   */
  function lengthContent(bytes32 articleId) internal view returns (uint256) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    uint256 _byteLength = StoreSwitch.getDynamicFieldLength(_tableId, _keyTuple, 2);
    unchecked {
      return _byteLength / 1;
    }
  }

  /**
   * @notice Get the length of content.
   */
  function _lengthContent(bytes32 articleId) internal view returns (uint256) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    uint256 _byteLength = StoreCore.getDynamicFieldLength(_tableId, _keyTuple, 2);
    unchecked {
      return _byteLength / 1;
    }
  }

  /**
   * @notice Get an item of content.
   * @dev Reverts with Store_IndexOutOfBounds if `_index` is out of bounds for the array.
   */
  function getItemContent(bytes32 articleId, uint256 _index) internal view returns (string memory) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    unchecked {
      bytes memory _blob = StoreSwitch.getDynamicFieldSlice(_tableId, _keyTuple, 2, _index * 1, (_index + 1) * 1);
      return (string(_blob));
    }
  }

  /**
   * @notice Get an item of content.
   * @dev Reverts with Store_IndexOutOfBounds if `_index` is out of bounds for the array.
   */
  function _getItemContent(bytes32 articleId, uint256 _index) internal view returns (string memory) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    unchecked {
      bytes memory _blob = StoreCore.getDynamicFieldSlice(_tableId, _keyTuple, 2, _index * 1, (_index + 1) * 1);
      return (string(_blob));
    }
  }

  /**
   * @notice Push a slice to content.
   */
  function pushContent(bytes32 articleId, string memory _slice) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    StoreSwitch.pushToDynamicField(_tableId, _keyTuple, 2, bytes((_slice)));
  }

  /**
   * @notice Push a slice to content.
   */
  function _pushContent(bytes32 articleId, string memory _slice) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    StoreCore.pushToDynamicField(_tableId, _keyTuple, 2, bytes((_slice)));
  }

  /**
   * @notice Pop a slice from content.
   */
  function popContent(bytes32 articleId) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    StoreSwitch.popFromDynamicField(_tableId, _keyTuple, 2, 1);
  }

  /**
   * @notice Pop a slice from content.
   */
  function _popContent(bytes32 articleId) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    StoreCore.popFromDynamicField(_tableId, _keyTuple, 2, 1);
  }

  /**
   * @notice Update a slice of content at `_index`.
   */
  function updateContent(bytes32 articleId, uint256 _index, string memory _slice) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    unchecked {
      bytes memory _encoded = bytes((_slice));
      StoreSwitch.spliceDynamicData(_tableId, _keyTuple, 2, uint40(_index * 1), uint40(_encoded.length), _encoded);
    }
  }

  /**
   * @notice Update a slice of content at `_index`.
   */
  function _updateContent(bytes32 articleId, uint256 _index, string memory _slice) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    unchecked {
      bytes memory _encoded = bytes((_slice));
      StoreCore.spliceDynamicData(_tableId, _keyTuple, 2, uint40(_index * 1), uint40(_encoded.length), _encoded);
    }
  }

  /**
   * @notice Get headerImageUrl.
   */
  function getHeaderImageUrl(bytes32 articleId) internal view returns (string memory headerImageUrl) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    bytes memory _blob = StoreSwitch.getDynamicField(_tableId, _keyTuple, 3);
    return (string(_blob));
  }

  /**
   * @notice Get headerImageUrl.
   */
  function _getHeaderImageUrl(bytes32 articleId) internal view returns (string memory headerImageUrl) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    bytes memory _blob = StoreCore.getDynamicField(_tableId, _keyTuple, 3);
    return (string(_blob));
  }

  /**
   * @notice Set headerImageUrl.
   */
  function setHeaderImageUrl(bytes32 articleId, string memory headerImageUrl) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    StoreSwitch.setDynamicField(_tableId, _keyTuple, 3, bytes((headerImageUrl)));
  }

  /**
   * @notice Set headerImageUrl.
   */
  function _setHeaderImageUrl(bytes32 articleId, string memory headerImageUrl) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    StoreCore.setDynamicField(_tableId, _keyTuple, 3, bytes((headerImageUrl)));
  }

  /**
   * @notice Get the length of headerImageUrl.
   */
  function lengthHeaderImageUrl(bytes32 articleId) internal view returns (uint256) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    uint256 _byteLength = StoreSwitch.getDynamicFieldLength(_tableId, _keyTuple, 3);
    unchecked {
      return _byteLength / 1;
    }
  }

  /**
   * @notice Get the length of headerImageUrl.
   */
  function _lengthHeaderImageUrl(bytes32 articleId) internal view returns (uint256) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    uint256 _byteLength = StoreCore.getDynamicFieldLength(_tableId, _keyTuple, 3);
    unchecked {
      return _byteLength / 1;
    }
  }

  /**
   * @notice Get an item of headerImageUrl.
   * @dev Reverts with Store_IndexOutOfBounds if `_index` is out of bounds for the array.
   */
  function getItemHeaderImageUrl(bytes32 articleId, uint256 _index) internal view returns (string memory) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    unchecked {
      bytes memory _blob = StoreSwitch.getDynamicFieldSlice(_tableId, _keyTuple, 3, _index * 1, (_index + 1) * 1);
      return (string(_blob));
    }
  }

  /**
   * @notice Get an item of headerImageUrl.
   * @dev Reverts with Store_IndexOutOfBounds if `_index` is out of bounds for the array.
   */
  function _getItemHeaderImageUrl(bytes32 articleId, uint256 _index) internal view returns (string memory) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    unchecked {
      bytes memory _blob = StoreCore.getDynamicFieldSlice(_tableId, _keyTuple, 3, _index * 1, (_index + 1) * 1);
      return (string(_blob));
    }
  }

  /**
   * @notice Push a slice to headerImageUrl.
   */
  function pushHeaderImageUrl(bytes32 articleId, string memory _slice) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    StoreSwitch.pushToDynamicField(_tableId, _keyTuple, 3, bytes((_slice)));
  }

  /**
   * @notice Push a slice to headerImageUrl.
   */
  function _pushHeaderImageUrl(bytes32 articleId, string memory _slice) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    StoreCore.pushToDynamicField(_tableId, _keyTuple, 3, bytes((_slice)));
  }

  /**
   * @notice Pop a slice from headerImageUrl.
   */
  function popHeaderImageUrl(bytes32 articleId) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    StoreSwitch.popFromDynamicField(_tableId, _keyTuple, 3, 1);
  }

  /**
   * @notice Pop a slice from headerImageUrl.
   */
  function _popHeaderImageUrl(bytes32 articleId) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    StoreCore.popFromDynamicField(_tableId, _keyTuple, 3, 1);
  }

  /**
   * @notice Update a slice of headerImageUrl at `_index`.
   */
  function updateHeaderImageUrl(bytes32 articleId, uint256 _index, string memory _slice) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    unchecked {
      bytes memory _encoded = bytes((_slice));
      StoreSwitch.spliceDynamicData(_tableId, _keyTuple, 3, uint40(_index * 1), uint40(_encoded.length), _encoded);
    }
  }

  /**
   * @notice Update a slice of headerImageUrl at `_index`.
   */
  function _updateHeaderImageUrl(bytes32 articleId, uint256 _index, string memory _slice) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    unchecked {
      bytes memory _encoded = bytes((_slice));
      StoreCore.spliceDynamicData(_tableId, _keyTuple, 3, uint40(_index * 1), uint40(_encoded.length), _encoded);
    }
  }

  /**
   * @notice Get the full data.
   */
  function get(bytes32 articleId) internal view returns (ArticleData memory _table) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    (bytes memory _staticData, EncodedLengths _encodedLengths, bytes memory _dynamicData) = StoreSwitch.getRecord(
      _tableId,
      _keyTuple,
      _fieldLayout
    );
    return decode(_staticData, _encodedLengths, _dynamicData);
  }

  /**
   * @notice Get the full data.
   */
  function _get(bytes32 articleId) internal view returns (ArticleData memory _table) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    (bytes memory _staticData, EncodedLengths _encodedLengths, bytes memory _dynamicData) = StoreCore.getRecord(
      _tableId,
      _keyTuple,
      _fieldLayout
    );
    return decode(_staticData, _encodedLengths, _dynamicData);
  }

  /**
   * @notice Set the full data using individual values.
   */
  function set(
    bytes32 articleId,
    address owner,
    uint64 createdAt,
    uint64 updatedAt,
    uint8 articleType,
    bytes32 category,
    string memory title,
    string memory kicker,
    string memory content,
    string memory headerImageUrl
  ) internal {
    bytes memory _staticData = encodeStatic(owner, createdAt, updatedAt, articleType, category);

    EncodedLengths _encodedLengths = encodeLengths(title, kicker, content, headerImageUrl);
    bytes memory _dynamicData = encodeDynamic(title, kicker, content, headerImageUrl);

    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    StoreSwitch.setRecord(_tableId, _keyTuple, _staticData, _encodedLengths, _dynamicData);
  }

  /**
   * @notice Set the full data using individual values.
   */
  function _set(
    bytes32 articleId,
    address owner,
    uint64 createdAt,
    uint64 updatedAt,
    uint8 articleType,
    bytes32 category,
    string memory title,
    string memory kicker,
    string memory content,
    string memory headerImageUrl
  ) internal {
    bytes memory _staticData = encodeStatic(owner, createdAt, updatedAt, articleType, category);

    EncodedLengths _encodedLengths = encodeLengths(title, kicker, content, headerImageUrl);
    bytes memory _dynamicData = encodeDynamic(title, kicker, content, headerImageUrl);

    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    StoreCore.setRecord(_tableId, _keyTuple, _staticData, _encodedLengths, _dynamicData, _fieldLayout);
  }

  /**
   * @notice Set the full data using the data struct.
   */
  function set(bytes32 articleId, ArticleData memory _table) internal {
    bytes memory _staticData = encodeStatic(
      _table.owner,
      _table.createdAt,
      _table.updatedAt,
      _table.articleType,
      _table.category
    );

    EncodedLengths _encodedLengths = encodeLengths(_table.title, _table.kicker, _table.content, _table.headerImageUrl);
    bytes memory _dynamicData = encodeDynamic(_table.title, _table.kicker, _table.content, _table.headerImageUrl);

    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    StoreSwitch.setRecord(_tableId, _keyTuple, _staticData, _encodedLengths, _dynamicData);
  }

  /**
   * @notice Set the full data using the data struct.
   */
  function _set(bytes32 articleId, ArticleData memory _table) internal {
    bytes memory _staticData = encodeStatic(
      _table.owner,
      _table.createdAt,
      _table.updatedAt,
      _table.articleType,
      _table.category
    );

    EncodedLengths _encodedLengths = encodeLengths(_table.title, _table.kicker, _table.content, _table.headerImageUrl);
    bytes memory _dynamicData = encodeDynamic(_table.title, _table.kicker, _table.content, _table.headerImageUrl);

    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    StoreCore.setRecord(_tableId, _keyTuple, _staticData, _encodedLengths, _dynamicData, _fieldLayout);
  }

  /**
   * @notice Decode the tightly packed blob of static data using this table's field layout.
   */
  function decodeStatic(
    bytes memory _blob
  ) internal pure returns (address owner, uint64 createdAt, uint64 updatedAt, uint8 articleType, bytes32 category) {
    owner = (address(Bytes.getBytes20(_blob, 0)));

    createdAt = (uint64(Bytes.getBytes8(_blob, 20)));

    updatedAt = (uint64(Bytes.getBytes8(_blob, 28)));

    articleType = (uint8(Bytes.getBytes1(_blob, 36)));

    category = (Bytes.getBytes32(_blob, 37));
  }

  /**
   * @notice Decode the tightly packed blob of dynamic data using the encoded lengths.
   */
  function decodeDynamic(
    EncodedLengths _encodedLengths,
    bytes memory _blob
  )
    internal
    pure
    returns (string memory title, string memory kicker, string memory content, string memory headerImageUrl)
  {
    uint256 _start;
    uint256 _end;
    unchecked {
      _end = _encodedLengths.atIndex(0);
    }
    title = (string(SliceLib.getSubslice(_blob, _start, _end).toBytes()));

    _start = _end;
    unchecked {
      _end += _encodedLengths.atIndex(1);
    }
    kicker = (string(SliceLib.getSubslice(_blob, _start, _end).toBytes()));

    _start = _end;
    unchecked {
      _end += _encodedLengths.atIndex(2);
    }
    content = (string(SliceLib.getSubslice(_blob, _start, _end).toBytes()));

    _start = _end;
    unchecked {
      _end += _encodedLengths.atIndex(3);
    }
    headerImageUrl = (string(SliceLib.getSubslice(_blob, _start, _end).toBytes()));
  }

  /**
   * @notice Decode the tightly packed blobs using this table's field layout.
   * @param _staticData Tightly packed static fields.
   * @param _encodedLengths Encoded lengths of dynamic fields.
   * @param _dynamicData Tightly packed dynamic fields.
   */
  function decode(
    bytes memory _staticData,
    EncodedLengths _encodedLengths,
    bytes memory _dynamicData
  ) internal pure returns (ArticleData memory _table) {
    (_table.owner, _table.createdAt, _table.updatedAt, _table.articleType, _table.category) = decodeStatic(_staticData);

    (_table.title, _table.kicker, _table.content, _table.headerImageUrl) = decodeDynamic(_encodedLengths, _dynamicData);
  }

  /**
   * @notice Delete all data for given keys.
   */
  function deleteRecord(bytes32 articleId) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    StoreSwitch.deleteRecord(_tableId, _keyTuple);
  }

  /**
   * @notice Delete all data for given keys.
   */
  function _deleteRecord(bytes32 articleId) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    StoreCore.deleteRecord(_tableId, _keyTuple, _fieldLayout);
  }

  /**
   * @notice Tightly pack static (fixed length) data using this table's schema.
   * @return The static data, encoded into a sequence of bytes.
   */
  function encodeStatic(
    address owner,
    uint64 createdAt,
    uint64 updatedAt,
    uint8 articleType,
    bytes32 category
  ) internal pure returns (bytes memory) {
    return abi.encodePacked(owner, createdAt, updatedAt, articleType, category);
  }

  /**
   * @notice Tightly pack dynamic data lengths using this table's schema.
   * @return _encodedLengths The lengths of the dynamic fields (packed into a single bytes32 value).
   */
  function encodeLengths(
    string memory title,
    string memory kicker,
    string memory content,
    string memory headerImageUrl
  ) internal pure returns (EncodedLengths _encodedLengths) {
    // Lengths are effectively checked during copy by 2**40 bytes exceeding gas limits
    unchecked {
      _encodedLengths = EncodedLengthsLib.pack(
        bytes(title).length,
        bytes(kicker).length,
        bytes(content).length,
        bytes(headerImageUrl).length
      );
    }
  }

  /**
   * @notice Tightly pack dynamic (variable length) data using this table's schema.
   * @return The dynamic data, encoded into a sequence of bytes.
   */
  function encodeDynamic(
    string memory title,
    string memory kicker,
    string memory content,
    string memory headerImageUrl
  ) internal pure returns (bytes memory) {
    return abi.encodePacked(bytes((title)), bytes((kicker)), bytes((content)), bytes((headerImageUrl)));
  }

  /**
   * @notice Encode all of a record's fields.
   * @return The static (fixed length) data, encoded into a sequence of bytes.
   * @return The lengths of the dynamic fields (packed into a single bytes32 value).
   * @return The dynamic (variable length) data, encoded into a sequence of bytes.
   */
  function encode(
    address owner,
    uint64 createdAt,
    uint64 updatedAt,
    uint8 articleType,
    bytes32 category,
    string memory title,
    string memory kicker,
    string memory content,
    string memory headerImageUrl
  ) internal pure returns (bytes memory, EncodedLengths, bytes memory) {
    bytes memory _staticData = encodeStatic(owner, createdAt, updatedAt, articleType, category);

    EncodedLengths _encodedLengths = encodeLengths(title, kicker, content, headerImageUrl);
    bytes memory _dynamicData = encodeDynamic(title, kicker, content, headerImageUrl);

    return (_staticData, _encodedLengths, _dynamicData);
  }

  /**
   * @notice Encode keys as a bytes32 array using this table's field layout.
   */
  function encodeKeyTuple(bytes32 articleId) internal pure returns (bytes32[] memory) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = articleId;

    return _keyTuple;
  }
}
