scrollback: 10000
procs:
  contracts:
    cwd: packages/contracts
    shell: wait-on -l ../../../dust/packages/world/deploys/31337/latest.json && pnpm run deploy:local
  register-app:
    cwd: packages/contracts
    shell: wait-on -l deploys/31337/latest.json && pnpm run manager:registerApp:local
